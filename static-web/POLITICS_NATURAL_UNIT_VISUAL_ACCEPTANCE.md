# Politics Natural Unit Visual Convergence — bounded acceptance note

Scope: Issue #116 presentation closure for the standard five-subject Natural Unit workspace only.

This slice does not reopen Politics Content, Learning, Projection, explicit Surface Mapping, Question Truth, Runtime, Evidence, Repair, Return, or learner U.

## Visual objective

Move the standard `PoliticsChapterRuntime` workspace closer to the accepted Dense Calm / Golden visual quality without changing learner payload or state timing:

- one clear dominant Natural Unit cognition surface;
- chapter rail stays contextual and quiet;
- Chengfeng remains the external first-round learning owner, but its companion region loses nested-card chrome;
- Xiao1000 remains verification inside the current Unit instead of reading like a second embedded application;
- default-visible learner text remains comfortably readable;
- Mac-wide composition uses width for cognition, not dashboard chrome;
- narrow fallback must not introduce document-level horizontal overflow.

## Exact write boundary

Allowed:
- `static-web/src/styles/politics-natural-unit-convergence.css`;
- route-level import in `static-web/src/pages/politics/[subject]/[chapter].astro`;
- focused browser/screenshot acceptance and workflow wiring.

Not allowed:
- `content/politics/**` semantic changes;
- Surface Mapping or explicit primitive changes;
- state trigger changes;
- Question / Evidence / Repair / Return changes;
- Shared Shell / global rail changes;
- Golden Marx C00 semantic/layout rewrite.

## Acceptance evidence

Representative Mac-wide screenshots:
- Marxism ch01;
- History ch01;
- Mao ch01;
- Xi ch01;
- Ethics-Law ch01.

Focused assertions:
- cognition column materially dominates companion width;
- source companion outer card chrome removed;
- Xiao1000 outer verification card chrome removed while answer options remain legitimate interactive boundaries;
- active chapter rail item is not a rounded card;
- Unit title >= 28px on representative Mac viewport;
- visible microcopy >= 15px;
- 390px History sample has no document-level horizontal overflow.

Final acceptance requires reading back the generated screenshots; green CI alone is insufficient.
