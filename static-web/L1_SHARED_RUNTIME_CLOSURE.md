# Bounded native-runtime integration — 2026-09-22

This is the receipt for PR #732, not a new control plane or final-system certification. Kian authorized selective integration, ordinary fixes and merge after proof; later narrowed content production out of scope and requested visible progress and bounded model use.

## Base and provenance

- Original root base: `1d319e9d75b51a4cca3e752da94a676f1235ff78` (`main`). Reconciled main: `03282542f13d8a24328fd114b427dfd7b57a7fef`; its 30 parallel Lexical content files are retained byte-for-byte and are not this runtime change. Static-web is in this repository; no separate Site ref is promoted.
- Shared candidate: PR #732, `f9beff15592b2becba3150dee4904788b0014873`.
- English candidate: PR #731, `36aeac2a7eeff718ccfffbbd70e91408a7cd9621`; native implementation/tests were reconciled selectively. The divergent #640 branch was not merged wholesale.
- Xizong: selected native repair from the #727/#730 scope, reconciled on current main. The incomplete old #730 working tree remains recoverable and was not treated as a finished candidate.
- Final identity is the exact PR head; do not infer merge/deployment from this receipt.

## Resulting behavior

- A same-command native English exposure update and Chat plan apply atomically. Later material evidence changes invalidate the plan in Home, including cross-tab changes, while native Resume remains available.
- Applied command receipts survive checkpoint recovery; retries do not replay native effects, and older commands cannot replace a newer applied instruction.
- Checkpoint capture/recovery uses subject-native adapters. Partial local state fills missing keys without shrinking durable history; English/Lexical are one dependency group while healthy siblings can recover independently. Corruption is retained and reported, never interpreted as zero study.
- The HTTP checkpoint writer requires an exact prior identity. An observed disk read alone does not prove that a browser inherited that history. Only an actually inherited local checkpoint may authorize changed local values; ambiguous or shadow-only recovery cannot mint that permission. Concurrent/stale writers fail without replacing the durable snapshot.
- English source identities match semantic material revisions; stale-source current inference stops while raw drafts, archived attempts and sealed historical outputs remain. Missing productive scores remain unknown. Existing layouts are retained.
- Xizong time estimates stay domain-specific/provisional where appropriate; question semantic revisions propagate across practice and packet consumers. Old evidence is retained without masquerading as current calibration.
- The worker uses at most one local model attempt for an unchanged task body. Claim/readback, process-group timeout, exact run/PR evidence and dead-claim handling prevent duplicate execution and false completion. No automatic Cloud dispatch or hourly retry loop remains in the candidate.

## Proof actually run

- Production-module integration: `node scripts/test-system-maturity-regression.mjs` — 14 cases, including actual HTTP concurrent writers and native partial-retirement ambiguity.
- `test-private-learner-checkpoint.mjs`, `test-private-daily-learning-packet.mjs`, `test-private-daily-learning-packet-bridge.mjs`, `test-private-control-relay.mjs`, `test-private-checkpoint-blur-flush.mjs` — passed.
- Shared continuation: 41 cases. Shared transaction orchestration: 10 cases, D1 native recovery and 1004 fingerprint vectors. These doubles are not substituted for native proof.
- English native closure: 77 cases; evidence fidelity 21; Forecast 25; source truth and native recovery checks passed.
- Xizong: seven targeted native module/guard checks passed, including fault rollback. No real exam attempts or canonical source material were produced.
- Worker: 25 isolated subprocess/claim/retry/timeout scenarios passed. Installed Mac worker is not certified by these tests.
- Real browser: existing synthetic English Writing Resume, source staleness and retained draft/archive recovery passed. A composed Home case proved same-command ready, same-page stale and real cross-tab storage-event stale while native Resume stayed usable. Disposable private directories and disabled relays were used.
- Final CI-driven repair: the live External Reading owner replaces its cached DOM projection, preventing duplicate identities from rejecting a valid command. The full existing private-control browser test then passed Chat command → relay → browser → Home → exact English Workspace without increasing its timeout. Revision-aware test fixtures were updated without weakening unknown/stale-source isolation.
- Full Astro build: 8,455 pages built successfully on the original runtime candidate. Reconciled-head full-site build is delegated to the existing PR CI; exact-head CI remains separately visible on the PR.

## Scope and operational readback

The write set is the exact PR diff: runtime owners, their targeted tests, the existing PR workflow, this receipt, Static-web Current and the watcher section of AGENTS. No `content/`, real learner state, source collection, release/hosting configuration or primary worktree state is edited. Task-only build outputs/dependencies are excluded from Git. The prior PR workflow is reused byte-for-byte: the current OAuth token lacks workflow-write scope, so native checkpoint and integration checks are wired through its existing private Packet test entrypoint without removing any original check.

Primary managed Current and the pre-existing dirty legacy workspace were left intact during preparation. Test servers were stopped after proof. No Production hosting operation was invoked. `kianos-2` optional morning collection was paused separately with its prior prompt/model/schedule preserved; the existing three-run Morning Brief trial was unchanged. Personal prompt simplification and actual Project installation are owned by Personal PR #41 and its deployment receipt.

No new permanent branch, schedule, learner ledger or universal maturity score is introduced. PR #732 is reused. Unique old branches/stashes and the incomplete #730 worktree are not cleanup candidates in this bounded change.

## Explicit remaining limits

- Source-update continuation through the existing Continue card and a subjective-score Chat return need a concrete interaction choice; no new JSON panel or speculative control path was introduced.
- Synthetic engineering proof is not Real-U calibration. Unpublished source knowledge remains source-gated.
- Project instruction readback is distinct from a fresh live teaching/continuation result.
- Main/PR identity must be re-read before promotion. Material UI redesign, Production hosting and mutation of real learner state remain outside this receipt.

After the PR's exact-head checks, merge the bounded repair or report its exact remaining blocker. Do not restart a broad audit or content lane.
