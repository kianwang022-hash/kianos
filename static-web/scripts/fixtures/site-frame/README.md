# Isolated full-site frame fixtures

No protected English exam material or real learner state. Reading, Cloze, Part B and Translation are newly authored synthetic text; Writing uses the admitted Current synthetic owner. Components/styles/Guides are the production implementations via the existing Astro alias pattern. Fixture pages and answers are not part of the production src/pages tree.

From static-web: `npx astro dev --root scripts/fixtures/site-frame --host 127.0.0.1 --port 4349`.
The Node integration browser script supplies exact synthetic answers only through these fixture endpoints. No Current content or policy is modified.
