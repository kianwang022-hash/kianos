// Fresh model-based browser proof. All attempts are SYNTHETIC, never learner U.
import fs from 'node:fs';
import path from 'node:path';
import os from 'node:os';
import http from 'node:http';
import assert from 'node:assert/strict';
import { chromium } from 'playwright';
import { buildPoliticsPracticeCatalogCurrent } from '../src/lib/politicsPractice.mjs';
import { PRACTICE_KEYS as K, politicsReviewPacket } from '../src/lib/politicsPracticeState.mjs';
const out=path.resolve(process.env.POLITICS_FRESH_EVIDENCE || '../qa/politics-final-fresh');fs.mkdirSync(out,{recursive:true});
const catalog=buildPoliticsPracticeCatalogCurrent('/'),site=path.resolve('dist');
const mime={'.html':'text/html','.js':'text/javascript','.mjs':'text/javascript','.json':'application/json','.css':'text/css','.svg':'image/svg+xml','.png':'image/png','.woff2':'font/woff2'};
const server=http.createServer((req,res)=>{try{let n=decodeURIComponent(new URL(req.url,'http://localhost').pathname);if(n.endsWith('/'))n+='index.html';const p=path.resolve(site,'.'+n);if(!p.startsWith(site+path.sep)||!fs.statSync(p).isFile())throw Error('missing');res.writeHead(200,{'Content-Type':mime[path.extname(p)]||'application/octet-stream'});res.end(fs.readFileSync(p));}catch{res.writeHead(404);res.end('not found');}});
await new Promise(r=>server.listen(0,'127.0.0.1',r));const base=`http://127.0.0.1:${server.address().port}`;
const browser=await chromium.launch({headless:true});const results=[];
async function check(name,fn){try{results.push({name,status:'PASS',detail:await fn()});}catch(e){results.push({name,status:'FAIL',error:String(e.stack||e)});}console.log(JSON.stringify(results.at(-1)));}
const state=page=>page.evaluate(keys=>Object.fromEntries(Object.entries(keys).map(([name,key])=>[name==='evidence'?'events':name,JSON.parse(localStorage.getItem(key)||'null')])),K);
async function context(init){const c=await browser.newContext({viewport:{width:1512,height:982}});if(init)await c.addInitScript(init);const page=await c.newPage();page.setDefaultTimeout(10000);return {c,page};}
const first=subject=>catalog.questions.find(q=>q.subject===subject&&q.unitKey&&q.type==='single');
async function start(page,q){await page.goto(`${base}/politics/practice/?question=${encodeURIComponent(q.id)}`);await page.locator('[data-filter-count]').selectOption('5');await page.locator('[data-learned-scope]').check();await page.locator('[data-start-session]').click();await page.locator('[data-question-options] button').first().waitFor();}
async function answer(page,q,outcome='WRONG'){if(outcome==='UNCERTAIN')await page.locator('[data-uncertain]').click();const choice=outcome==='WRONG'?q.options.find(o=>!q.answer.includes(o.label))?.label||q.answer[0]:q.answer;for(const l of choice)await page.locator(`[data-option="${l}"]`).click();await page.locator('[data-submit]').click();}
const visibleResult=page=>page.locator('[data-submitted-result]').waitFor({state:'visible'});
const shot=(page,name)=>page.screenshot({path:path.join(out,name+'.png'),fullPage:true});

for(const subject of ['marxism','history','mao','xi','ethics_law'])await check('single-workbench-clean-WU-continue:'+subject,async()=>{
 const {c,page}=await context();const q=first(subject),requests=[];page.on('request',r=>{if(r.url().includes('/practice-review/'))requests.push(r.url());});
 try{await start(page,q);assert.equal(requests.length,0);assert.equal(await page.locator('[data-result-answer]').textContent(),'');await answer(page,q,subject==='xi'?'UNCERTAIN':'WRONG');await visibleResult(page);const before=await state(page),a=before.attempts.units[q.unitKey].attempts[q.id];assert.equal(a.outcome,subject==='xi'?'UNCERTAIN':'WRONG');assert.equal(before.session.ids[before.session.index],q.id);assert.equal(new URL(page.url()).pathname,'/politics/practice/');assert.equal((await page.locator('[data-takeaway]').textContent()).trim(),q.refined.takeaway.trim());assert.equal((await page.locator('[data-chat-explanation]').textContent()).trim(),q.refined.chatExplanation.trim());await page.locator('[data-note]').fill('SYNTHETIC fresh-audit note');await page.locator('[data-cause="options"]').click();await shot(page,subject+'-submitted');
 const style=await page.locator('[data-cause="options"]').evaluate(b=>({text:b.textContent,foreground:getComputedStyle(b).color,background:getComputedStyle(b).backgroundColor,opacity:getComputedStyle(b).opacity,classes:b.className}));
 await page.locator('[data-next-question]').click();const after=await state(page);assert.deepEqual(after.attempts.units[q.unitKey].attempts[q.id],a);assert.equal(after.meta.notes[q.id],'SYNTHETIC fresh-audit note');assert.equal(after.meta.causes[q.id],'options');assert.equal(after.session.index,Math.min(1,after.session.ids.length-1));assert.equal(after.events.length,1);assert.equal(after.events[0].question_id,q.id);assert.deepEqual(after.events[0].source_context,a.source_context);return {question:q.id,outcome:a.outcome,answerRequests:requests.length,selectedCauseStyle:style};}finally{await c.close();}
});

await check('corrupt-array-meta-fails-closed',async()=>{const {c,page}=await context(()=>localStorage.setItem('kianos-politics-practice-meta-v1','[]'));try{await page.goto(base+'/politics/practice/');await page.waitForTimeout(200);assert.equal(await page.locator('[data-practice-error]').isVisible(),true);assert.equal(await page.locator('[data-start-session]').isDisabled(),true);assert.equal(await page.evaluate(()=>localStorage.getItem('kianos-politics-practice-meta-v1')),'[]');}finally{await c.close();}});

await check('failed-evidence-write-locks-attempt-and-idempotent-retry',async()=>{const {c,page}=await context(()=>{const set=Storage.prototype.setItem;Storage.prototype.setItem=function(k,v){if(k==='kianos-politics-evidence-v1'&&window.__denyEvidence)throw new DOMException('synthetic quota','QuotaExceededError');return set.call(this,k,v);};});try{const q=first('history');await start(page,q);await page.evaluate(()=>window.__denyEvidence=true);await answer(page,q);await page.locator('[data-retry-save]').waitFor({state:'visible'});const pending=await state(page);assert.ok(pending.session.pending);assert.equal(pending.session.index,0);assert.equal(await page.locator('[data-practice-error]').isVisible(),true);const original=pending.attempts.units[q.unitKey].attempts[q.id];await page.evaluate(()=>window.__denyEvidence=false);await page.locator('[data-retry-save]').click();await visibleResult(page);const saved=await state(page);assert.equal(saved.events.length,1);assert.equal(saved.session.pending,null);assert.deepEqual(saved.attempts.units[q.unitKey].attempts[q.id],original);await page.reload();await visibleResult(page);assert.equal((await state(page)).events.length,1);}finally{await c.close();}});

await check('exact-optional-source-return-survives-passive-navigation',async()=>{const {c,page}=await context();try{const q=first('history');await start(page,q);await answer(page,q);await page.locator('[data-return-unit]').waitFor({state:'visible'});const href=await page.locator('[data-return-unit]').getAttribute('href');const old=await state(page);await page.locator('[data-return-unit]').click();await page.waitForTimeout(450);const units=page.locator('[data-politics-unit]');if(await units.count())await units.first().scrollIntoViewIfNeeded();await page.waitForTimeout(300);let saved=await state(page);assert.equal(saved.last.question_id,q.id);const target=new URL(saved.last.href,base),expected=new URL(href,base);assert.equal(target.searchParams.get('practiceSession'),expected.searchParams.get('practiceSession'));assert.equal(target.searchParams.get('practiceQuestion'),q.id);assert.deepEqual(saved.events,old.events);await page.reload();await page.waitForTimeout(350);saved=await state(page);assert.equal(saved.last.question_id,q.id);await shot(page,'optional-source-return');await page.locator(`a[href*="session=${encodeURIComponent(saved.session.id)}"][href*="question=${encodeURIComponent(q.id)}"]`).first().click();await visibleResult(page);assert.deepEqual((await state(page)).events,old.events);}finally{await c.close();}});

await check('Review-batch-export-is-read-only-and-keeps-first-evidence',async()=>{const q=first('history');const a={question_id:q.id,outcome:'STABLE',selected:q.answer,correct_answer:q.answer,study_day:'2026-09-18',observed_at:'2026-09-18T01:00:00.000Z'};const snap={attempts:{units:{[q.unitKey]:{attempts:{[q.id]:a}}}},meta:{latestOutcome:{[q.id]:'WRONG'},notes:{[q.id]:'SYNTHETIC'},causes:{[q.id]:'options'}},events:[{event_id:'synthetic-retry',question_id:q.id,unit_id:q.unitId,outcome:'WRONG',observed_at:'2026-09-18T02:00:00.000Z'}],last:null,errors:[]};const bytes=JSON.stringify(snap),p=politicsReviewPacket(catalog,snap);assert.ok(JSON.stringify(p).includes(a.observed_at));politicsReviewPacket(catalog,snap);assert.equal(JSON.stringify(snap),bytes);});

await check('five-subject-actual-Unit-states-no-second-attempt',async()=>{
 const {c,page}=await context();const observations=[];
 try{for(const [subject,chapter,index,primitive] of [['marxism','ch01',1,'PARALLEL_SET'],['marxism','ch01',2,'DIRECTED_SEQUENCE'],['history','ch01',4,'HIERARCHY'],['history','ch02',1,'STATEMENT'],['history','ch02',3,'PARALLEL_SET'],['mao','ch01',2,'PARALLEL_SET'],['xi','ch01',2,'STATEMENT'],['ethics_law','ch06',2,'PARALLEL_SET'],['ethics_law','ch06',3,'TIMELINE'],['ethics_law','ch06',4,'STATEMENT']]){
 await page.goto(`${base}/politics/${subject}/${chapter}/#unit-${index}`);await page.waitForTimeout(250);const section=page.locator(`#unit-${index}`);await section.waitFor({state:'visible'});assert.ok(await section.locator(`[data-surface-primitive="${primitive}"]`).count());assert.equal(await page.locator('[data-politics-question],[data-politics-submit]').count(),0);assert.ok(await section.locator('a[href*="/politics/practice/"]').count());assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth+2),false);await shot(page,`${subject}-${chapter}-unit${index}`);observations.push({subject,chapter,index,primitive});
 }
 await page.goto(`${base}/politics/marxism/ch02/#unit-1`);await page.waitForTimeout(250);assert.equal(await page.locator('[data-politics-question]').count(),0);assert.ok(await page.locator('#unit-1 a[href*="/politics/practice/"]').count());await shot(page,'marxism-k03-optional-checkpoint');
 await page.goto(`${base}/politics/marxism/ch00/`);await page.waitForTimeout(200);assert.ok(await page.getByText('20 秒闭卷收口（可选）',{exact:false}).count()+await page.getByText('20秒闭卷收口（可选）',{exact:false}).count());await shot(page,'marxism-ch00-relations');return observations;
 }finally{await c.close();}
});

await check('real-browser-restart-preserves-private-first-and-events',async()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),'politics-fresh-private-'));let c;try{c=await chromium.launchPersistentContext(dir,{headless:true,viewport:{width:1512,height:982}});let page=await c.newPage();const q=first('mao');await start(page,q);await answer(page,q);await visibleResult(page);const before=await state(page);assert.equal(before.events.length,1);await c.close();c=null;c=await chromium.launchPersistentContext(dir,{headless:true,viewport:{width:1512,height:982}});page=await c.newPage();await page.goto(`${base}/politics/practice/?session=${encodeURIComponent(before.session.id)}&question=${encodeURIComponent(q.id)}`);await visibleResult(page);const after=await state(page);assert.deepEqual(after.attempts,before.attempts);assert.deepEqual(after.events,before.events);assert.equal(after.session.id,before.session.id);}finally{if(c)await c.close();fs.rmSync(dir,{recursive:true,force:true});}});

await check('Review-and-Home-no-auto-Chat-no-stable-debt',async()=>{const {c,page}=await context();try{const q=first('history');await start(page,q);await answer(page,q,'STABLE');await visibleResult(page);const before=await state(page);assert.equal(before.events?.length||0,0);await page.goto(base+'/politics/review/');await page.locator('[data-review-empty]').waitFor({state:'visible'});assert.equal(await page.locator('[data-review-question]').count(),0);await shot(page,'review-stable');await page.goto(base+'/politics/');await page.waitForTimeout(200);assert.equal(await page.locator('[data-review-copy]').count(),0);await shot(page,'home');const after=await state(page);assert.deepEqual(after.attempts,before.attempts);assert.deepEqual(after.events,before.events);}finally{await c.close();}});

await check('Review-WU-packet-only-on-explicit-click',async()=>{const {c,page}=await context(()=>{window.__copies=[];Object.defineProperty(navigator,'clipboard',{value:{writeText:async text=>window.__copies.push(text)},configurable:true});});try{const q=first('history');await start(page,q);await answer(page,q);await visibleResult(page);await page.goto(base+'/politics/review/');await page.locator('[data-review-question]').first().waitFor();assert.equal((await page.evaluate(()=>window.__copies)).length,0);await page.locator('[data-review-copy]').click();const once=await state(page);await page.locator('[data-review-copy]').click();const twice=await state(page);assert.deepEqual(twice,once);const packets=await page.evaluate(()=>window.__copies.map(JSON.parse));assert.equal(packets.length,2);assert.equal(packets[0].first_attempts[0].attempt.question_id,q.id);assert.equal(packets[0].review_policy.no_follow_up_is_valid,true);await shot(page,'review-Wrong');}finally{await c.close();}});

await check('missing-or-wrong-review-payload-fails-closed-before-submit',async()=>{const {c,page}=await context();try{const q=first('history');await page.route('**/practice-review/**',route=>route.fulfill({status:503,body:'synthetic unavailable'}));await start(page,q);await answer(page,q);await page.locator('[data-practice-error]').waitFor({state:'visible'});let s=await state(page);assert.equal(s.attempts?.units?.[q.unitKey]?.attempts?.[q.id],undefined);assert.equal(await page.locator('[data-submitted-result]').isVisible(),false);await page.unroute('**/practice-review/**');await page.route('**/practice-review/**',async route=>{const p=await (await route.fetch()).json();p.id='SYNTHETIC-WRONG-OBJECT';await route.fulfill({json:p});});await page.locator('[data-submit]').click();await page.locator('[data-practice-error]').waitFor({state:'visible'});s=await state(page);assert.equal(s.attempts?.units?.[q.unitKey]?.attempts?.[q.id],undefined);}finally{await c.close();}});

await check('unrelated-catalog-revision-preserves-current-task-session',async()=>{const {c,page}=await context();try{const q=first('history');await start(page,q);await answer(page,q);await visibleResult(page);const before=await state(page);await page.evaluate(key=>{const s=JSON.parse(localStorage.getItem(key));s.revision='SYNTHETIC-unrelated-content-update';localStorage.setItem(key,JSON.stringify(s));},K.session);await page.reload();await visibleResult(page);let after=await state(page);assert.deepEqual(after.attempts,before.attempts);assert.equal(after.session.id,before.session.id);await page.evaluate(key=>{const s=JSON.parse(localStorage.getItem(key));s.taskRevisions[s.ids[0]]='SYNTHETIC-actual-question-change';localStorage.setItem(key,JSON.stringify(s));},K.session);await page.reload();await page.locator('[data-practice-error]').waitFor({state:'visible'});assert.equal(await page.locator('[data-start-session]').isDisabled(),true);assert.deepEqual((await state(page)).attempts,before.attempts);}finally{await c.close();}});

await check('review-payload-version-and-task-identity-are-independent',async()=>{const {c,page}=await context();try{const q=first('history');await page.route('**/practice-review/**',async route=>{const p=await (await route.fetch()).json();p.taskRevision='SYNTHETIC-WRONG-TASK';await route.fulfill({json:p});});await start(page,q);await answer(page,q);await page.locator('[data-practice-error]').waitFor({state:'visible'});assert.equal((await state(page)).attempts?.units?.[q.unitKey]?.attempts?.[q.id],undefined);await page.unroute('**/practice-review/**');await page.route('**/practice-review/**',async route=>{const p=await (await route.fetch()).json();p.revision='SYNTHETIC-new-explanation-same-task';await route.fulfill({json:p});});await page.locator('[data-submit]').click();await visibleResult(page);const a=(await state(page)).attempts.units[q.unitKey].attempts[q.id];assert.equal(a.source_context.content_revision,'SYNTHETIC-new-explanation-same-task');assert.equal(a.source_context.task_revision,q.taskRevision);}finally{await c.close();}});

await browser.close();await new Promise(r=>server.close(r));
const report={schema:'kianos.politics.fresh-browser-audit.v1',platform:process.platform,node:process.version,synthetic:true,learner_U:'UNTESTED',results,passed:results.filter(r=>r.status==='PASS').length,failed:results.filter(r=>r.status==='FAIL').length};fs.writeFileSync(path.join(out,'browser-results.json'),JSON.stringify(report,null,2));console.log(JSON.stringify({passed:report.passed,failed:report.failed,platform:report.platform}));if(report.failed&&process.env.POLITICS_FRESH_ALLOW_FAILURES!=='1')process.exitCode=1;
