name: LexicalOS Historical Round Bulk Triage

on:
  push:
    branches:
      - main
    paths:
      - 'tools/lexical_historical_round_bulk_triage.py'
      - 'tools/lexical_historical_round_from_file.py'
      - 'tools/lexical_historical_round_owner_surfaces.py'
      - 'content/lexical/audit/migration-integrity/r22-authority.md'
      - '.github/workflows/lexical-historical-round-triage.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  r22-bulk-triage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read frozen R22 authority and bulk-triage Current
        shell: bash
        run: |
          python tools/lexical_historical_round_owner_surfaces.py \
            content/lexical/audit/migration-integrity/r22-authority.md \
            --legacy-repo kianwang022-hash/kianos-legacy \
            --comment-id 5584653524 \
            --audit-id R22_5001_5300_CORE_EXPANSION_CONTRAST \
            --expected-core 83 \
            --expected-expansion 98 \
            --expected-contrast 63 \
            --output /tmp/r22-bulk-triage.json \
            --summary /tmp/r22-bulk-triage-summary.md

      - name: Publish summary
        if: always()
        shell: bash
        run: |
          if [ -f /tmp/r22-bulk-triage-summary.md ]; then
            cat /tmp/r22-bulk-triage-summary.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo '## R22 bulk triage summary unavailable' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload triage artifact
        uses: actions/upload-artifact@v4
        with:
          name: lexical-r22-bulk-triage
          path: |
            /tmp/r22-bulk-triage.json
            /tmp/r22-bulk-triage-summary.md
          if-no-files-found: error
          retention-days: 14
