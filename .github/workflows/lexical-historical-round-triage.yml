name: LexicalOS Historical Round Bulk Triage

on:
  push:
    branches:
      - main
    paths:
      - 'tools/lexical_historical_round_bulk_triage.py'
      - 'tools/lexical_historical_round_from_file.py'
      - 'tools/lexical_historical_round_owner_surfaces.py'
      - 'tools/lexical_historical_round_from_lock.py'
      - 'content/lexical/audit/migration-integrity/r25-authority-lock.json'
      - '.github/workflows/lexical-historical-round-triage.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  r25-bulk-triage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read locked R25 authority and bulk-triage Current
        shell: bash
        run: |
          python tools/lexical_historical_round_from_lock.py \
            content/lexical/audit/migration-integrity/r25-authority-lock.json \
            --legacy-repo kianwang022-hash/kianos-legacy \
            --comment-id 5585869440 \
            --audit-id R25_5901_6200_CORE_EXPANSION_CONTRAST \
            --expected-core 101 \
            --expected-expansion 92 \
            --expected-contrast 80 \
            --output /tmp/r25-bulk-triage.json \
            --summary /tmp/r25-bulk-triage-summary.md

      - name: Publish summary
        if: always()
        shell: bash
        run: |
          if [ -f /tmp/r25-bulk-triage-summary.md ]; then
            cat /tmp/r25-bulk-triage-summary.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo '## R25 bulk triage summary unavailable' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload triage artifact
        uses: actions/upload-artifact@v4
        with:
          name: lexical-r25-bulk-triage
          path: |
            /tmp/r25-bulk-triage.json
            /tmp/r25-bulk-triage-summary.md
          if-no-files-found: error
          retention-days: 14
