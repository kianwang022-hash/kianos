name: Runner Diagnostic

on:
  pull_request:
    paths:
      - '.github/workflows/runner-diagnostic.yml'

jobs:
  ubuntu-slim-probe:
    runs-on: ubuntu-slim
    steps:
      - name: Probe runner allocation
        run: echo "RUNNER_DIAGNOSTIC_STARTED"
