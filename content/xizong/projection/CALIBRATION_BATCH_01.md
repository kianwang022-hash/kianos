# Xizong Cognitive Projection — Calibration Batch 01 Receipt

Status: **STRUCTURAL CALIBRATION COMPLETE — SCHEMA FREEZE / RUNTIME ADOPTION NOT YET CLAIMED**  
Baseline inspected: `main@2f1974bf8b270393626fef95472c64dbe1c81f7d`  
Contract: `content/xizong/projection/PROJECTION_CONTRACT.md`

## Scope

Seven heterogeneous Current Blocks were compiled as reference-heavy Projection assets:

```text
A1 B1   mechanism chain + formula language + framework
A1 B7   valve inference + four-valve comparison + boundary
A1 B10  stability-first decision algorithm + ECG/perfusion boundary
A1 B11  vicious loop + framework + wet/cold coordinate
A2 R1   measurement/mechanics + Visual/Precision/Connection enrichment
A3 B1   spatial/directional/measurement/control map + source handoff
A3 B5   five-variable coordinate + priority algorithm + multi-source provenance
```

No medical Core, question truth, learner evidence or Runtime was modified.

## Calibration findings

### 1｜One Block cannot be one shape — CONFIRMED

The real Current Blocks require multiple simultaneous semantic objects. B7 alone needs inference + comparison + boundary + framework; B11 needs a causal/feedback model plus a separate wet/cold decision coordinate. The schema therefore keeps `objects[]` and does not define one `projection_shape` per Block.

### 2｜Role and geometry must remain separate — CONFIRMED

The same semantic role can have different useful Mac geometry, and a geometry change should not redefine medical truth. Calibration therefore stores e.g. `COMPARE + MATRIX`, `MAP + AXES`, `CHAIN + LOOP`, `MAP + FORMULA_STRIP` as separate dimensions.

### 3｜Reference-heavy compilation is viable — CONFIRMED

All seven assets bind to Current through source registry + exact refs/selectors. Canonical KP Core is not copied into Projection. JSON learning support uses JSON pointers; Markdown uses exact blockquote/heading/marker/anchor selectors; optional cue/pathway items use reviewed IDs.

### 4｜Optional enrichment does not require schema symmetry — CONFIRMED

A2 R1 binds reviewed Visual, Precision and Connection items through `enrichment_bindings[]`. A1 and A3 remain valid without matching sidecars. No placeholder enrichment files were introduced.

### 5｜Narrow external-source authority can stay provenance-only — CONFIRMED

A3 B5 references its admitted external-source contract through `provenance_bindings[]`. Canonical B5 remains the learner-facing content owner; the Projection does not copy MSD/NCBI material or widen the admitted scope.

### 6｜Multi-state reuse is structurally expressible — CONFIRMED AT ASSET LEVEL

Each Block can expose different `views` over the same bound cognition:

```text
BLOCK_ORIENT
LOGIC_GROUP_ORIENT
EXTERNAL_HANDOFF
KP_RECALL_FRONT / REVEAL
GROUP_CLOSURE
BLOCK_RECALL_FRONT / REVEAL
```

No separate first-pass/Recall copy of medical content was created. Future second-pass views remain intentionally unpopulated.

### 7｜Mac/Dense Calm requirements are preserved as semantic layout intent — CONFIRMED AT CONTRACT LEVEL

Assets may indicate `PRIMARY_STAGE / SECONDARY_STAGE / CONTEXT` and cognitive geometry, but do not encode pixels, card styling, colors or typography. This leaves enough structure for Mac-wide spatial rendering while keeping Kian's visual preferences in Product/UI owners.

## Eight hard tests — current evidence state

| Test | Calibration result |
|---|---|
| Identity | **STRUCTURAL PASS** — all seven assets bind named Current System/Block owners and canonical LG/KP sets |
| Source freshness | **DESIGN PASS / EXECUTION PENDING** — path+blob SHA pinned; validator still needs to enforce STALE behavior |
| No invention | **STRUCTURAL PASS** — only Current selectors/reviewed enrichment IDs used; absent enrichment remains absent |
| Optional enrichment | **PASS** — A2 enrichments coexist with unenriched A1/A3 without schema branching |
| Multi-object Block | **PASS** — B1/B7/B10/B11/R1/B1/B5 all fit one BlockProjection schema |
| Stateful reuse | **STRUCTURAL PASS** — views reuse refs; no duplicate medical summaries authored |
| Neutral-front safety | **DESIGN PASS / RUNTIME VALIDATION PENDING** — protected views declare `NEUTRAL_FRONT`; whole-workspace validator still required |
| R10 Content Change Absorption | **STRUCTURAL PASS / MUTATION TEST PENDING** — no named-topic schema branch introduced; automated mutation/recompile regression still required |

## What is NOT yet accepted

Do not claim any of the following from this batch alone:

- full A1/A2/A3 Projection compilation;
- frozen production schema;
- SystemProjection calibration;
- loader/parser/validator implementation;
- Astro/Runtime consumption;
- screenshot or Mac aesthetic acceptance;
- S/K/L/P/R/E/U promotion;
- real learner validation.

## Next dependency

Before mass compilation, close a small **Projection validator + schema freeze** step:

```text
validate JSON shape
→ resolve every FIELD/OWNER/INDEX selector exactly
→ verify pinned source freshness
→ verify Current-owned object IDs / LG/KP scope
→ enforce null/no-invention policy
→ enforce neutral-front object visibility
→ run one bounded source-change mutation test
→ freeze schema v1
```

Only then compile the remaining A1/A2/A3 Blocks.

## Separate Runtime blocker

Question attempts must become append-preserved and phase-aware before multi-pass question productization. This is not solved by Cognitive Projection and remains a Runtime/Evidence compatibility task before the new question Runtime is implemented.
