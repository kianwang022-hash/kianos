# B Projection P2 — Atomic Compilation Receipt

Status: **MATERIALIZED — P3 SHARED VALIDATION / ADVERSARIAL CLOSURE PENDING**  
Scope: `B — Digestive / Metabolic / Endocrine / Tumor`  
Execution: GitHub Issue `#155`

This receipt records Projection construction only. It does **not** claim P PASS, Runtime adoption, browser answer-leak safety, Mac visual acceptance, learner progress, or U.

## Atomic landing

The P2 landing adds in one repository tree:

```text
B SystemProjection = 1
B BlockProjection  = 38
B assets           = 39

shared validator v1.2 wrapper
+ frozen v1.1 validator retained as validate_projection_v1.py
+ B-specific adversarial suite
+ Projection manifest coverage update
```

Expected repository-wide declarative coverage after the landing:

```text
Systems            = 4
Blocks             = 76
Projection assets  = 80
canonical KP IDs   = 1405
```

The previously validated A1/A2/A3 assets are not rewritten by this compile.

## B owner bindings

B System Projection binds:

- `system.json` for mission, mother model, spine, controls, variables, relations, Failure Modes, judgment axes and canonical Block labels;
- the accepted B Learning owner for default learner route, non-gating PSRs and final System reconstruction.

B Block Projections bind:

- canonical Block center question through `OWNER_REF`;
- `first_pass_focus / stop_line / recall_spine / logic_groups` through exact Learning-owner `FIELD_REF`;
- a safe `LOGIC_GROUP_SET` owner view that exposes only accepted LG IDs + labels.

The B adapter resolves stable Blocks directly from:

```text
D* → d-d1-d23/
M* → m-m1-m10/
G* → g-g1-g5/
```

and resolves LG identity / range / learner order from the accepted B Learning owner. It does **not** synthesize `system.logic_index`.

## B-specific Projection safety

B intentionally differs from the older A baseline in two places:

1. `BLOCK_ORIENT` does **not** render the whole B canonical Block Markdown. B Block Markdown is too rich to become a second Lecture; first-view orientation stays bounded to problem/focus/stop/safe LG map while complete canonical KP Core remains available after legitimate Recall Reveal.
2. `SYSTEM_RECALL_FRONT` carries no B medical answer object. A future renderer supplies only the generic attempt instruction; answer-bearing B reconstruction objects appear only after Reveal.

PSRs remain absent from `SYSTEM_GUIDE`; the dedicated `PARTIAL_SYSTEM_RECONSTRUCTION` view exposes accepted PSR semantics only when Runtime later selects an eligible checkpoint.

## P2 boundary

P2 is complete only as materialization. P remains `UNTESTED / ACTIVE` until P3 executes the shared validator + legacy regressions + B adversarial cases and P4 performs fresh Projection acceptance.
