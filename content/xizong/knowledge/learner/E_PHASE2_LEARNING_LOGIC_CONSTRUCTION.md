# E Phase 2 — Learning Logic Construction

Status: **BUILDER CANDIDATE · READY FOR FRESH INDEPENDENT L AUDIT**  
Date: 2026-09-20  
Scope: E — Reproductive / Breast  
Target gate: **L — Learning Logic**

## 1｜Authority / boundary

This construction started from current `main@99b7754a6c19001ad65419fdfb08c38b75926624` after E K had already closed as:

```text
S1 = PASS
K  = PASS_AFTER_BOUNDED_REPAIR
identity = 20 Blocks / 212 canonical KP
```

Accepted Knowledge owner:

`content/xizong/knowledge/systems/e-reproductive-breast/system.json`

This phase may decide learner order, Source-contact granularity, Logic Groups, retrieval closure, compression and repair return. It may **not** rewrite Source truth, Block/KP identity, accepted K relations, Projection/UI, Runtime/Evidence, official-question membership or learner progress.

Historical E Guide topology remains reference-only and was not used as the learner route.

## 2｜Fresh learning problem

E has two accepted Knowledge trunks but several distinct first-learning shapes:

- reproductive control / time sequencing;
- tissue / compartment identity and barrier logic;
- morphology / lineage discrimination;
- transport / drainage / source control;
- breast evidence / stage / receptor / treatment objectives;
- one low-connectivity male developmental surgical interface.

The learning problem is therefore not “walk the old chapter DAG” and not “one workflow for every Block”.

The candidate uses:

> **no hard inside-E prerequisite edges + one low-switching default route + explicit benefits/reactivation + Block-specific Source-contact classes**

The zero-hard-edge claim is intentionally auditable, not presumed true.

## 3｜Route decision

Default first-pass guidance:

```text
SR1 → SR2 → SR3 → SR4 → SR5 → SR6
→ E1 → E2 → E3 → E4 → E5 → E6 → E7 → E8
→ E9 → E10 → E11
→ E12 → E13 → E14
```

This order minimizes topic/source switching and preserves useful continuity. It is **not medical ontology** and **not hard legality**.

### Hard-edge delete test

A hard prerequisite is allowed only if the later Block intentionally consumes an earlier E model, does not rebuild it from its own accepted Primary Source, and bounded reactivation would be insufficient.

Builder attack rejected several tempting hard edges:

- `SR1 → SR2/SR3/SR4/SR5/SR6`: SR1 is useful common control language, but each later physiology Block still has its own Primary Source.
- `E1 → E2`: transformation-zone geography helps, but E2 can form from its own pathology Source with bounded E1 reactivation.
- `E5 → E6`: prior mole/villus learning sharpens trophoblastic discrimination, but E6 owns the decisive villus/invasion/spread model it needs.
- `SR6 → E11/E12`: normal breast/drainage structure helps, but E11/E12 contain enough accepted organ-specific Primary to form without a whole-Block gate.
- `E12 → E13`: pathology identity improves evidence/stage interpretation but E13 has its own accepted surgery Source.
- `E13 → E14`: treatment uses T/N/M and receptor outputs, but bounded local reactivation is sufficient; E14's accepted Source still owns the treatment model.

Thus current candidate hard edges = **0**. A fresh auditor must actively attack this for under-gating.

## 4｜Source-contact decision

Compiled candidate:

`content/xizong/knowledge/learner/e-reproductive-breast-learning.json`

Source-contact accounting:

```text
WHOLE_BLOCK_SOURCE    = 11 Blocks
NATURAL_SOURCE_UNITS  = 9 Blocks
INTEGRATION_PRIMARY   = 0 Blocks
```

Natural Source units are used only where there is real multi-center / multi-source / visual density, including SR1, SR3, SR4, SR6, E8, E10, E12, E13 and E14.

Hard rule:

> **Logic Group count never determines Source-contact count.**

No design creates one MarginNote/iPad trip per LG.

## 5｜Logic-Group reconstruction

Exact builder accounting:

```text
20 Blocks
212 canonical KP
67 Logic Groups
KP assigned exactly once = 212 / 212
duplicate KP membership  = 0
missing KP membership    = 0
```

Branch accounting:

```text
SR1–SR6        61 KP / 21 LG
E1–E8          71 KP / 23 LG
E9–E11         27 KP /  9 LG
E12–E14        53 KP / 14 LG
-------------------------
TOTAL          212 KP / 67 LG
```

Groups were built around local learner problems rather than equal-size quotas. The largest groups are allowed only where the current KPs genuinely answer one morphology/treatment problem; there are no ceremonial one-KP LGs.

Representative shapes:

- SR3: target-tissue effects → cycle initiation → aligned time-axis reconstruction → whole-cycle recovery.
- E2: epithelial layer model → HPV molecular interface → invasion identity → spread/stage reconstruction.
- E6: trophoblast identity → **invasion vs metastatic competence separation** → presentation/treatment boundary → PSTT reverse classification.
- E8: germ-cell maturity → yolk-sac marker/morphology → sex-cord stromal identity.
- E11: stasis/infection → early source control → abscess drainage → inflammatory-cancer discrimination.
- E12–E14: pathology identity → extent/evidence/receptor → local/regional/systemic treatment objectives.

The E6 bounded K repair is therefore preserved in Learning rather than re-collapsed.

## 6｜Visual contract

Current Source truth is preserved:

```text
verified source pages      = SR1–SR6, E12
ready in project source    = E1–E8
VISUAL_SOURCE_GAP          = E9, E10, E11
partial source pages       = E13, E14
```

The candidate marks **24 LGs** as visual-dependent where spatial/morphologic evidence is material.

Rule:

> `visual_required` requires real original-Source visual formation when the relation is genuinely visual; it does not itself create a separate Source trip.

For E9/E10/E11 gaps and E13/E14 partial visual support, Learning may preserve the requirement but may not certify mastery from prose.

## 7｜Compression

Candidate compression ladder:

```text
212 KP
→ 67 LG
→ 20 Block Recall
→ 3 non-gating Partial-System Reconstructions
→ final E System Reconstruction
```

Three optional PSRs:

1. **CONTROL / TIME RELAY** — SR1–SR6;
2. **TISSUE IDENTITY / BOUNDARY** — E1–E8;
3. **BREAST STRUCTURE → CONTROL** — SR6 + E10–E14.

They are compression opportunities, not readiness gates or extra Memory debt.

Final E reconstruction must preserve:

- control/time vs tissue/compartment identity as two coupled trunks;
- local invasion separate from metastatic competence/spread;
- E9 as a low-connectivity developmental surgical interface;
- explicit Source/visual/version boundaries.

## 8｜Question boundary

Exact official E System question membership remains:

```text
S2 = UNTESTED
```

Therefore the normal Xizong first-pass chain may build through final E System Reconstruction, but the trusted official E System question sweep remains fail-closed until S2 is separately accepted.

No question membership is inferred from Block, Outline or model similarity.

## 9｜Self-adversarial builder checks

Passed at builder-candidate level:

- exact 212/212 KP partition readback;
- 67/67 LG structural readback;
- no duplicate/missing membership;
- zero hard-edge graph and default-route separation;
- Source-contact continuity check;
- no per-LG Source bouncing;
- E6 invasion/spread repair preserved;
- visual gaps remain live;
- Current-Study stage/treatment version boundaries preserved;
- prostate, generic HPV/infection and tumor-general Primary remain external;
- no Content / Projection / Runtime semantics promoted into L.

This is **not independent acceptance**.

## 10｜Candidate verdict / next gate

```text
L = UNTESTED / CANDIDATE_READY_FOR_FRESH_INDEPENDENT_AUDIT
```

Next required action:

`content/xizong/knowledge/learner/E_PHASE3_FRESH_INDEPENDENT_L_AUDIT_BRIEF.md`

Do not start E Content Realization before fresh L acceptance.
