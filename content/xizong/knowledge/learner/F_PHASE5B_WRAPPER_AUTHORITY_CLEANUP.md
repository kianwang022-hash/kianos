# F Phase 5B — Wrapper / Authority Cleanup

Status: **PASS / CLOSED**  
Date: 2026-09-20  
Scope: F canonical Block wrapper semantics only  
Upstream: accepted F K + L + Phase 5A Content role realization

## Result

Fresh wrapper readback established:

```text
canonical F Blocks                         = 9
frontmatter prerequisites before           = 9 / 9
frontmatter next_blocks/next_system before = 9 / 9
accepted inside-F hard prerequisite edges  = 0
accepted NATURAL_SOURCE_UNITS Blocks       = 5
accepted WHOLE_BLOCK_SOURCE Blocks         = 3
accepted INTEGRATION_PRIMARY Blocks        = 1

after cleanup:
frontmatter prerequisites                  = 0 / 9
frontmatter next_blocks/next_system        = 0 / 9
natural-unit wrappers aligned              = 5 / 5
whole-block wrappers preserved             = 3 / 3
integration-primary ownership-first        = 1 / 1
canonical KP count                         = 121
medical Core rewrites                      = 0
LG identity/membership changes             = 0
Source-contact topology changes            = 0
```

## 1｜Route authority cleanup

All nine canonical F Block Markdown owners still carried historical frontmatter fields such as:

```text
prerequisites:
next_blocks:
next_system:
```

Those fields predated accepted F Learning and could be read as a second readiness graph.

Accepted Learning truth is:

```text
inside-F hard prerequisite edges = 0
default F1→F9 route = low-switching guidance only
benefits-from relation = bounded reactivation, not legality
```

Phase 5B removed the stale route fields from all nine Block frontmatters.

This does not delete medical relationships. Source/Recall/Apply interfaces remain in `study_refs`, ownership sections and medical Core.

## 2｜Natural Source-unit wrapper alignment

The five accepted natural-unit Blocks previously still advertised a generic pattern of whole-Block Lecture consumption before broad Recall.

They now follow the accepted Source stop-lines:

### F1

```text
threat / exposure + weighted clues
→ local retrieval / closure
→ decontamination boundary
→ local retrieval / closure
→ elimination + antidote + final algorithm
→ remaining retrieval
→ Block Recall
```

Selected exact toxicologic mappings remain original-Source-review dependent.

### F2

```text
AChE / M / N / CNS mechanism + severity
→ local retrieval / closure
→ decontamination + antidote/control layers
→ local retrieval / closure
→ delayed-risk timeline + bounded opioid/sedative interfaces
→ remaining retrieval
→ Block Recall
```

Delayed polyneuropathy / rebound exact wording remains fail-closed.

### F4

```text
systemic burn mechanism + area/depth/severity original visuals
→ local retrieval / closure
→ time-phase + fluid resuscitation original Source
→ local retrieval / closure
→ wound/infection + inhalation-airway/final routing
→ local retrieval / closure
→ Block Recall
```

The complete first-day burn-fluid formula remains original-Source visual truth.

### F6

```text
post-anesthesia positioning + routine recovery / suture-drain timing
→ local retrieval / closure
→ postoperative time/system/evidence deviations
→ local retrieval / closure
→ incision class/healing + wound complications
→ local retrieval / closure
→ Block Recall
```

The verified 1–4 d vs 2–7 d postoperative timing remains unchanged.

### F8

```text
local/regional technique + local-anesthetic mechanism/agent selection
→ local retrieval / closure
→ neuraxial space/level + complications/special approaches
→ local retrieval / closure
→ Block Recall
```

Neuraxial/adjacent-anatomy relations remain original-visual-Source dependent.

No per-LG device bouncing was introduced.

## 3｜Whole-block Source behavior preserved

F3 / F5 / F7 remain `WHOLE_BLOCK_SOURCE`.

They retain:

```text
bounded orientation
→ one compact coherent original-Source contact
→ Block LG retrieval / closure
→ Block Recall
```

They were not split merely to make every Block look the same.

## 4｜F9 integration-primary preserved

F9 already had the correct body-level shape:

```text
Ownership Split
→ external-owner Recall / Apply
→ targeted Source return for laparoscopy Micro Primary
→ targeted Source return for surface-lesion morphology
→ local retrieval / closure
```

Only its stale route frontmatter was removed.

Transplant, tumor-general, cancer-pain and pharmacology truth remain with their real owners.

## 5｜Learner-state assumption repair

Concrete stale assumptions were repaired without changing medicine:

- F6's F5 study-ref action: `recall_prerequisite` → `recall_apply`;
- F7 no longer says F5 must already be completed before F7; F5 language is reusable and locally reactivatable;
- F8 no longer assumes F7 has already been completed; F7 compression is helpful but not hard readiness;
- F2 external M1 language is now explicitly reusable with local reactivation when unstable.

This keeps repository order distinct from private learner state.

## 6｜Question and Source boundary

Unchanged:

- Lecture-attached TTSX releases only at real Source boundaries with reviewed binding;
- no binding → no invented release;
- S2 exact official F System-question membership remains **UNTESTED**;
- no Question→Knowledge relation is inferred from Block/Outline/LG position.

## 7｜Preserved invariants

Unchanged:

```text
Blocks                         = 9
canonical KP                   = 121
Logic Groups                   = 40
inside-F hard prerequisites    = 0
Source modes                   = 3 whole + 5 natural + 1 integration
PSR                            = 2 non-gating
F9                             = ownership-first integration-primary
accepted K/L                   = unchanged
Source gaps/conflicts          = unchanged
Projection / Runtime / Evidence = frozen
learner progress               = no claim
```

## 8｜Next

```text
Phase 5C — 40-LG Content sufficiency / density + Recall-front audit
→ targeted repair only where concrete defects exist
→ Phase 5D fresh independent whole-F Content closure
```

Content remains **ACTIVE / NOT ACCEPTED**.
