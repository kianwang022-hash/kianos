# Lexical Sol Final Reconciliation — o1225–o1424

Status: **RECONCILED / READY_FOR_BOUNDED_CORRECTIVE_IMPLEMENTATION**

Role: single executable semantic authority for all 16 durable Independent-Audit delta owners inside the already mechanically implemented `o1225–o1424` window.

Baseline read for this reconciliation: branch `lexical/retro-audit-debt-20260915@48b174f22f5b0cbdd14a37482e3d48190e5f62b4`, after post-audit closure through `o1224`.

Authority inputs:

- `content/lexical/semantic-audit/o1225-o1424.audit.md` (`HOLD_FOR_SOL`);
- `content/lexical/CONTENT_ASSET_CONTRACT.md`;
- `content/lexical/CONTENT_EXECUTION.md`;
- Current Natural Owners and exact Sense / Relation / spelling dependencies for all 16 Audit deltas.

PASS owners remain untouched. This artifact does not activate `o1425+` and does not grant learner-state or Acceptance Truth.

## Terminal accounting

```text
Audit delta owners                              16 / 16
  Production NO_CHANGE false-pass               12
  Production UPGRADE identity-risk               4

Resolved by current drift / no mutation          2
  o1282 delicate
  o1295 dense

Actual in-window Word mutations                  14
Remote Word mutation                              1  (dispatch@o5772)

Identity actions
  NEW_SEMANTIC_BRANCH                             2
    demographic group/audience noun               1
    depress press/push-down verb                  1
  REACTIVATE historical stable senses             4  (directly adverb use-space)
  DEPRECATE synthetic conflated sense             1  (directly:2a91...)
  terminal ESCALATE_IDENTITY                      0

Relation actions
  reuse + add reciprocal Word view                3 existing Relation IDs
  new spelling-variant Relation                   1  (dispatch ↔ despatch)
```

## Drift closure before implementation

Two Audit identity risks are already satisfied by later Current and must **not** be mechanically replayed:

- `o1282 delicate`: Current now exposes historical stable `sense:delicate:6ead081b7497547f` (fragile), `sense:delicate:f01790ebf8855a06` (sensitive/subtle handling), and `sense:delicate:6bfa6fdceaa55907` (easily hurt) as active learner branches alongside fine/refined `sense:delicate:470156a661315a66`. Audit target is already met.
- `o1295 dense`: Current now exposes historical `sense:dense:1854cd5e9dd85ee2` as active L3/register-marked informal “slow to understand/stupid”, separate from physical density. Audit target is already met.

These two owners are included in final integrated readback but are excluded from the executor write set.

## Executor rules

1. Mutate only the 14 in-window target Words listed below plus exact remote `dispatch@o5772`, exact existing Relation owners named below, the one new dispatch/despatch Relation owner, and identity/collocation registries required by those mutations.
2. Generate exactly **two** new semantic sense IDs: `demographic` group/audience and `depress` press/push-down. No other new sense is authorized.
3. Reuse the four historical `directly` adverb stable IDs; do not create replacement IDs.
4. Preserve both stable Main Word IDs `word:despatch` and `word:dispatch`. The learner default is standard `dispatch`; `despatch` is an accepted chiefly British spelling variant. Represent that with one new spelling Relation + reciprocal Word views/Form overlays. Do not delete, alias, retire, transplant, or collapse either Word owner.
5. Preserve the existing dessert/desert and discreet/discrete Relation identities; add the missing reciprocal Word views and Word refs only. Do not create duplicate confusable Relations.
6. Low/reference material remains historical truth unless explicitly promoted/demoted below.
7. Final acceptance requires all 16 Audit deltas closed, the two no-mutation Current-drift owners re-read as PASS, exact write set, exact new-sense count, Relation closure, Natural Owner audit, JSON validation, `git diff --check`, Astro build, and successful branch push.

## A. Production `NO_CHANGE` false-pass corrections — 12

### `o1234 dean` — CORE_ONLY
Keep active university `sense:dean:1cf8c44e44525542` and senior-member `sense:dean:716d96540bb151e4`. Remove generic “church” from learner Core. Preserve deprecated/reference `sense:dean:cce4ecb10a645715` (head of College of Cardinals) without promoting it.

### `o1289 democratic` — CASE-CONDITIONED CORE
Current already has proper Form truth on `sense:democratic:4b5735e652f65028` with `surface_lemma = Democratic`. Preserve it. Rewrite Core so lowercase `democratic` organizes democracy/fair-participation meanings; represent US-party `Democratic` as the capitalized case-conditioned branch rather than an unconditional lowercase meaning.

### `o1290 demographic` — NEW_SEMANTIC_BRANCH + PLURAL USAGE
Create a learner-worthy noun branch `a demographic` = a group/segment of people sharing demographic characteristics, especially an audience/market segment. Keep adjective `sense:demographic:49ba0038db9654c6`. Demote existing `sense:demographic:bf75987673505578` from active singular “a statistic” Study to reference/history rather than redefining that stable identity into the group noun. Add word-level learner Form/usage truth: plural `demographics` = demographic characteristics/data of a population. Reattach/remove `demographics of a region` from the old singular-statistic branch so the learner model no longer teaches that collocation as evidence for `a demographic = a statistic`.

### `o1310 depress` — NEW_SEMANTIC_BRANCH
Create one lower but learner-worthy transitive verb branch: press or push something down, e.g. `depress the button/brake pedal`. Keep emotional, economic/value-reduction, and force/activity-reduction branches separate.

### `o1315 descend` — REUSE_EXISTING_STABLE
Keep `sense:descend:40926fced4905d60`. Define semantically as be descended/derive from an ancestor or earlier source. Keep `descend from sb/sth` as its construction/collocation; do not let the pattern string stand in for the definition.

### `o1333 dessert` — REUSE TWO EXISTING RELATIONS
Do not create a new Relation. Add reciprocal learner entrypoint from `dessert` for both existing owners already reachable from `desert`:

1. `deep:confusables:desert:37f6c7139bdae3dc` → `content/lexical/relations/by-id/9b/9bf6c3302549c20a0c952ce1086d99b18a105fcffe4a8a6965a272cd8140a7a3.json`;
2. `confusable:horizontal:2d30fd29b904135a1a8a` → `content/lexical/relations/by-id/e0/e080418600d4297269d63ca76977cd6dab4e02ba8fd8534ccbd8e8da49758016.json`.

Append reciprocal `word:dessert / o1333` Word views and matching `relation_refs` indices `0` and `1`. Preserve Relation IDs and existing desert views.

### `o1341 detain` — REATTACH COLLOCATION
Move `detain sb for questioning` from delay `sense:detain:46bab15bdabe5988` to custody/confinement `sense:detain:bf34b296fb1458d1`. Keep delay syntax on genuine delay contexts.

### `o1367 die` — REATTACH/BROADEN GRADUAL-INTENSITY USE
Move `die down` from stop-burning/functioning `sense:die:4ada21c34b3c5f3e` to gradual reduction/disappearance `sense:die:73b168734e38508d`; broaden that branch enough to cover becoming less strong, loud, active, or obvious. Keep `die out` / `die away` compatible with the same gradual-loss family.

### `o1370 difference` — REATTACH IDIOM
Move `make a difference` from simple unlike-ness `sense:difference:bb38106d4ef452fc` to significant effect/change `sense:difference:5fadcb074e345437`; broaden that L2 branch from bare “a significant change” to significant effect/change so the idiom is semantically licensed. Keep `difference between A and B` on unlike-ness.

### `o1380 dilemma` — BROADEN EXISTING STABLE
Reuse `sense:dilemma:d3a92deed1585c7f`. Define as a difficult situation or choice between alternatives, typically involving undesirable trade-offs. Equality of badness is not a defining requirement. Preserve existing phraseology.

### `o1418 discrete` — REUSE EXISTING RELATION
Do not create a new Relation. Reuse `confusable:0ad78cc0b87bdf566e62` at `content/lexical/relations/by-id/9d/9d44734b084b2c3d5328b5511c7a7a9c24a94a3f65883d478b246f1a33ed8fe5.json`. Append reciprocal `word:discrete / o1418` Word view and one `relation_ref` index `0`, reversing the learner-facing source/target boundary so `discrete` can lead to `discreet`.

### `o1419 discretion` — REATTACH IDIOM
Move `at one's discretion` from careful/prudent behavior `sense:discretion:8d2611cbcc575490` to freedom/right to decide `sense:discretion:2aee49affa47548e`. Keep `use discretion` on careful judgment.

## B. Production `UPGRADE` identity-risk resolutions — 4

### `o1282 delicate` — CURRENT_ALREADY_SATISFIES / NO MUTATION
Final readback only. Required historical fragile/sensitive branches are already active under their own stable IDs. Do not rewrite.

### `o1295 dense` — CURRENT_ALREADY_SATISFIES / NO MUTATION
Final readback only. Historical informal-intelligence `sense:dense:1854cd5e9dd85ee2` is already active and separate. Do not rewrite.

### `o1329 despatch` + remote `o5772 dispatch` — PRESERVE BOTH WORD IDs + NEW SPELLING RELATION
Keep both Word owners and every existing stable sense. Standard learner form is `dispatch`; `despatch` is accepted chiefly British variant. Add one genuine cross-Word spelling-variant Relation with reciprocal views and Form overlays on active senses in both owners. No sense transplant, alias, Word retirement, or stable-ID merge. Existing `despatch` verb overlay may be normalized to this frozen spelling-variant identity truth.

### `o1395 directly` — HISTORICAL SPLIT/UNMERGE + SYNTHETIC RETIREMENT
Retire/deprecate synthetic conflated active `sense:directly:2a91a53595e044ff` because it mixes several semantically distinct ordinary adverb uses.

Reactivate and expose these historical stable adverb branches separately:

- `sense:directly:53012ca40f5055ad` — directly/straight, without deviation or interruption;
- `sense:directly:5e7bc196cbe55971` — directly, without intermediary/intervention;
- `sense:directly:9fddaeae00b354a1` — candidly/forthrightly/directly in speech;
- `sense:directly:f3021c44e8615a31` — immediately/very soon.

For each reactivated ID, clear prior `merged_into_sense_id`. Keep conjunction `sense:directly:c19aa67890d15f64 = as soon as` deprecated/reference-only and out of default Core. Core should organize direct path/manner, no intermediary, candidness, and immediacy without collapsing them back into one identity.

## New-branch ledger — exactly 2

1. `demographic@o1290` — noun: group/audience segment sharing demographic characteristics.
2. `depress@o1310` — verb: press/push down a button, pedal, etc.

## Relation ledger

Existing Relation owners modified in place:

1. `deep:confusables:desert:37f6c7139bdae3dc` — add dessert reciprocal view;
2. `confusable:horizontal:2d30fd29b904135a1a8a` — add dessert reciprocal view;
3. `confusable:0ad78cc0b87bdf566e62` — add discrete reciprocal view.

New Relation owner:

4. exactly one `dispatch ↔ despatch` spelling-variant Relation; standard `dispatch`, chiefly British `despatch`; reciprocal views on `o1329` and remote `o5772`.

## Exact executor Word write set

In-window:

`1234, 1289, 1290, 1310, 1315, 1329, 1333, 1341, 1367, 1370, 1380, 1395, 1418, 1419`

Remote:

`5772 dispatch`

No write: `1282 delicate`, `1295 dense` — final readback only.

## Acceptance readback checklist

- all 16 Audit deltas closed;
- `delicate` and `dense` current-drift closures re-read PASS without mutation;
- exact 14 in-window Word writes + remote `o5772`, no other Word write;
- exactly two new semantic branches;
- directly historical split active; synthetic broad branch deprecated; conjunction remains reference-only;
- democratic Core no longer flattens capitalized Democratic into ordinary lowercase identity;
- demographic singular group noun and plural demographics usage truth are coherent; old statistic identity not taught as ordinary singular noun;
- dean church claim removed from Core while church-specific reference preserved;
- descend definition semantic, not pattern string;
- detain/die/difference/discretion phrase attachment corrected;
- dilemma broadened without new sense;
- dessert has reciprocal refs/views for both existing desert relations;
- discrete has reciprocal ref/view for existing discreet relation;
- dispatch/despatch spelling Relation reciprocal and both frozen Word IDs preserved;
- Relation manifest count increments by exactly one;
- stable identity/reference closure PASS;
- changed JSON PASS;
- Natural Owner audit PASS;
- `git diff --check` PASS;
- Astro build PASS.
