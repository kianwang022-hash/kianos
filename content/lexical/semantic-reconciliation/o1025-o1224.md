# Lexical Sol Final Reconciliation — o1025–o1224

Status: **RECONCILED / READY_FOR_BOUNDED_CORRECTIVE_IMPLEMENTATION**

Role: single executable semantic authority for the 12 durable Independent-Audit delta owners inside the already mechanically implemented `o1025–o1224` window.

Baseline read for this reconciliation: branch `lexical/retro-audit-debt-20260915@133675f3a0672a48e8dc893d4ca9c89da8cf3ce9`, after post-audit closure through `o1024`.

Authority inputs:

- `content/lexical/semantic-audit/o1025-o1224.audit.md` (`HOLD_FOR_SOL`);
- `content/lexical/CONTENT_ASSET_CONTRACT.md`;
- `content/lexical/CONTENT_EXECUTION.md`;
- current Natural Owners for all 12 delta owners and exact stable identity readback where identity materially affects execution.

PASS owners remain untouched. This artifact does not activate `o1225+` and does not grant learner progress or acceptance.

## Terminal accounting

```text
Audit delta owners                         12 / 12
  Production NO_CHANGE false-pass           9
  Production UPGRADE refine                 3

Final identity actions
  NEW_SEMANTIC_BRANCH                        3
    critical academic-evaluative adjective  1
    cross breed/mix verb                     1
    custom customer-patronage noun           1
  REACTIVATE_EXISTING_STABLE                 0 required on current branch
  terminal ESCALATE_IDENTITY                 0

Cross-word Relation actions
  new Relation IDs                           0
  remote Word writes                         0
```

Audit-time restore requests for `cow` and `culminate` have already landed mechanically in Current: `sense:cow:9145f75b2f695bef` and `sense:culminate:f637600946f85e9c` are active today. Corrective execution therefore finishes their learner hierarchy/patterns without repeating identity mutation.

## Executor rules

1. Apply only the exact desired states below.
2. Generate exactly three new stable sense IDs, under `critical`, `cross`, and `custom`; no other new stable sense is authorized.
3. No new Relation ID and no out-of-window Word mutation is authorized.
4. Existing stable IDs named below must be reused in place.
5. `customer → custom` remains the existing word-family fact; retarget it to the newly created custom patronage branch and restore it to a useful/publishable relation. Do not create a second edge and do not leave the tradition anchor.
6. Reference/low-register material is preserved unless this authority explicitly changes its learner prominence; no stable identity is deleted merely to simplify Study.
7. Final acceptance requires exact 12-Word write set, exactly three new stable sense IDs, zero new Relation IDs, stable identity/reference closure, Natural Owner audit, changed JSON validation, `git diff --check`, Astro build, and integrated readback.

## A. Production `NO_CHANGE` false-pass corrections — 9

### `o1041 consumption` — REUSE_EXISTING_STABLE / BROADEN SCOPE
Reuse `sense:consumption:5ceeb60a670d59f6`. Broaden it from economics-only “buying and using goods/services” to the ordinary act of consuming/using something, including eating or drinking. Economics remains a specialization/examples of this branch. Keep `sense:consumption:e74d84779df45993` separately as “amount of something used.” Core must organize act-of-consumption vs amount consumed without claiming unsupported branches.

### `o1047 contemporary` — REUSE_EXISTING_STABLE / BROADEN NOUN
Reuse noun `sense:contemporary:d9b6443ac2365cbd`. Definition becomes a person living or existing in the same period as another; same-age contemporaries are a narrower possible case, not the defining condition. Keep present-day adjective and same-period adjective distinct and unchanged.

### `o1055 continual` — CORE CORRECTION
Keep active `sense:continual:460dc201885e5975` exactly centered on repeated/recurring/frequent over time, often with interruptions. Remove “without interruption” from Core. Preserve the useful nuance boundary: `continuous` centers uninterrupted continuity; `continual` centers recurrence, without teaching an absolute impossibility rule.

### `o1098 corporation` — WRONG-FORM ATTACHMENT
Remove `corporate + law` / `corporate law` from corporation noun `sense:corporation:f868073fca7f51da`. Do not replace it with another `corporation` collocation and do not create a semantic branch to justify adjective syntax.

### `o1108 cost` — CONSTRUCTION OWNERSHIP
Keep all existing stable senses. Add one reusable learner construction `cost sb sth` covering both:

- price: `The trip cost me $X`;
- loss/consequence: `The mistake cost him his job`.

The construction is word-level and may name both price verb `sense:cost:9209151211cb5e31` and loss verb `sense:cost:4a4f87be411053ca` in its boundary/definition; do not merge those semantic branches merely because syntax overlaps.

### `o1142 crawl` — CONSTRUCTION-FIRST FAMILIAR NEW USE
Add one word-level learner construction `be crawling with sb/sth` = be completely full/covered with a type of person/thing. Do not create a new semantic sense and do not attach it as if it were literal hands-and-knees motion.

### `o1160 critical` — NEW_SEMANTIC_BRANCH
Current registry contains no active or reference branch matching academic/evaluative `critical`. Create one adjective branch, learner-worthy L1/L2: involving careful, reasoned evaluation, analysis, or judgment. Anchor `critical thinking`, `critical analysis`, and analogous academic uses there. Keep negative/fault-finding `sense:critical:fbd4f6e7b23351a0`, crucial `0c167...`, dangerous-condition `fc8f...`, and technical threshold `dc242...` separate.

### `o1164 cross` — CONSTRUCTION + NEW_SEMANTIC_BRANCH
1. Remove `cross + sth + off` from physical traversal `sense:cross:761831ef41a65b7f`; expose word-level construction `cross sth off (a list)` = remove/delete an item from a list.
2. Current registry has no historical verb branch for breed/mix. Create one verb branch `cross A with B` = breed/cross/mix two different types to produce a hybrid. Move the existing `cross + sth + with + sth` collocation from physical-intersection `sense:cross:6f3975cf8d2c5a0f` to this new verb branch.
3. Preserve noun hybrid `sense:cross:3e66403e052950ee` as a separate noun result identity.

### `o1200 custom` — NEW_SEMANTIC_BRANCH / EXPANSION
Create one noun branch, L2/Expansion, mainly British/commerce: business or patronage given to a shop/company by its customers. Useful phraseology includes `take your custom elsewhere` and `most of our custom comes from ...`. Keep tradition/practice `sense:custom:2eece3fc2c7854e5`, plural customs/border service `a8c608...`, and custom-made adjective `bea8c...` distinct. Do not force patronage into default Core merely to support the family relation.

## B. Production `UPGRADE` refinements — 3

### `o1134 cow` — CURRENT IDENTITY ALREADY ACTIVE / CORE HIERARCHY
Current already exposes active verb `sense:cow:9145f75b2f695bef` = frighten/intimidate and the construction `cow sb into doing sth`. Preserve that stable ID. Rebuild default Core around mature female cattle/animal + intimidate verb. Keep offensive UK noun `sense:cow:1d17d2f6354e56f1` active only at low/register-marked level if retained, but remove it from the organizing Core and do not let it define the mental model.

### `o1179 culminate` — CURRENT IDENTITY ALREADY ACTIVE / PATTERN FIX
Current already exposes `sense:culminate:f637600946f85e9c`. Keep it as the sole ordinary active learner verb: reach a final, highest, or climactic stage/point. Correct transitivity to intransitive and make `culminate in sth` the primary reusable construction. Core must be semantic (“reach a final/highest/climactic point”), not merely a pattern string. Preserve `sense:culminate:2790a065c71c54fc` deprecated/reference-only; do not reactivate the causative/transitive branch.

### `o1201 customer` — RETARGET EXISTING WORD-FAMILY FACT
Preserve `deep:word_family:customer:33d2f77f1becba1c` and source `sense:customer:c6c2681a9c725d73`. Retarget its `target_sense_id` from tradition `sense:custom:2eece3fc2c7854e5` to the new `custom` patronage branch created by `o1200`. Rewrite learner boundary to explain `customer` person ↔ mainly British `custom` patronage/business. Restore the edge from the current mechanically demoted false-anchor state to a verified learner-useful relation; do not create a duplicate fact.

## New-branch ledger — exactly 3

1. `critical@o1160` — academic/evaluative adjective: careful, reasoned evaluation/judgment.
2. `cross@o1164` — verb: breed/mix/cross A with B to produce a hybrid.
3. `custom@o1200` — noun, mainly British commerce: customer patronage/business.

## Acceptance readback checklist

- exact Word write set: `1041,1047,1055,1098,1108,1134,1142,1160,1164,1179,1200,1201`;
- no remote Word write;
- exactly three new stable semantic branches and no other generated senses;
- no new Relation owner;
- `consumption:5cee...` covers ordinary consumption/use/eating/drinking; `e74d...` remains amount-used;
- contemporary noun `d9b...` means same period, not necessarily same age;
- continual Core does not claim uninterrupted continuity;
- `corporate law` is absent from corporation learner collocations;
- explicit `cost sb sth` construction exists;
- `be crawling with sb/sth` construction exists;
- critical academic/evaluative branch is active and owns critical thinking/analysis phraseology;
- `cross sth off` is construction-owned; breed/mix verb is active and owns `cross A with B`; noun hybrid remains separate;
- cow offensive noun is not in default Core; intimidate verb remains active;
- culminate ordinary verb is intransitive, semantic Core is corrected, causative old sense remains reference-only;
- custom patronage branch is active at Expansion level and is the target of the existing customer→custom family fact;
- Natural Owner / identity / reference closure PASS;
- changed JSON PASS;
- `git diff --check` PASS;
- Astro build PASS.
