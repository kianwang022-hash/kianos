# Lexical Semantic Reconciliation — o4875–o5374

Status: **SOL RECONCILIATION COMPLETE / IMPLEMENTATION AUTHORIZED AFTER MAIN LANDING**

Role: canonical bounded semantic authority reconciling the complete Production Fresh Semantic Review and Independent Semantic Audit for one **500-owner** execution package. This file freezes semantic/identity decisions; it does not itself mutate lexical owners or claim learner K/P/R/E/U acceptance.

## Scope and source authority

Package: `o4875–o5374` — **500 owners**.

Production handoffs:
- `content/lexical/semantic-review/o4875-o5124.md` — 250/250 reviewed, 54 Production `UPGRADE`, 196 `NO_CHANGE`, `BLOCKED=0`.
- `content/lexical/semantic-review/o5125-o5374.md` — 250/250 reviewed, 48 Production `UPGRADE`, 202 `NO_CHANGE`, `BLOCKED=0`.

Independent Audit coverage consumed:
- `content/lexical/semantic-audit/o4825-o5024.audit.md` for `o4875–o5024`.
- `content/lexical/semantic-audit/o5025-o5224.audit.md` for `o5025–o5224`.
- `content/lexical/semantic-audit/o5225-o5424.audit.md` for `o5225–o5374`.

Machine-compiled accounting authority:

`content/lexical/execution/preflight/o4875-o5374.reconciliation-compile.json`

That artifact is generated by `tools/lexical_compile_reconciliation_package.py`; it is accounting/transport truth only. Semantic precedence remains: Current Truth → this reconciliation → accepted Audit desired state → accepted Production desired state.

## Frozen accounting

```text
scope owners:                               500
Production UPGRADE before Audit:            102
Audit FLIP_TO_NO_CHANGE:                      4
retained Production semantic sources:        98
Audit additions beyond Production:           61
unique semantic source-owner union:          159
terminal semantic BLOCKED after Sol:           0
terminal IDENTITY_RISK after Sol:              0
authorized out-of-range Word writes:           8
```

Audit verdict accounting compiled for this package:

```text
FLIP_TO_NO_CHANGE   4
FLIP_TO_UPGRADE    59
REFINE_UPGRADE     13
IDENTITY_RISK       4  → all four resolved below
```

The four Production upgrades frozen back to `NO_CHANGE` are `o5126,o5254,o5291,o5356`. They are not semantic mutation sources unless a new Current defect is independently demonstrated.

The **exact 159-owner semantic source set**, per-owner Audit verdict map, source provenance, and ten internal receipt counts are frozen in `o4875-o5374.reconciliation-compile.json`. Executors must consume that compiled set rather than maintain a second hand-copied source list.

## Internal checkpoints

The package is one Chat/execution work unit. The ten 50-owner intervals are only resumable transport / rollback / receipt checkpoints:

```text
o4875–o4924
o4925–o4974
o4975–o5024
o5025–o5074
o5075–o5124
o5125–o5174
o5175–o5224
o5225–o5274
o5275–o5324
o5325–o5374
```

A continuous/resumable runner must skip already-PASS receipts on retry and return to Sol only for a genuinely new ambiguity outside this file.

## Authorized out-of-range Word writes

Exactly eight out-of-range Word owners may be touched, solely to close the same accepted Relation/Form identity reciprocally or repair a stale anchor named by this reconciliation:

- `consequently@o1020` — accepted `therefore ↔ consequently` contrast.
- `disclose@o1410` — accepted `uncover ↔ disclose` relation.
- `full@o2069` — accepted `total ↔ full` relation.
- `integrity@o2586` — accepted `unity ↔ integrity` relation.
- `join@o2671` — accepted `unite ↔ join` relation.
- `so@o4524` — accepted `thus ↔ so` contrast.
- `suppose@o4825` — existing `think ↔ suppose` Relation; reciprocal visibility and stale target-anchor repair only.
- `whole@o5416` — accepted `unit ↔ whole` relation; reciprocal visibility only despite the target lying beyond this package frontier.

`shallow@o4375` and `stream@o4731` appear in the machine dependency candidate set only because the first Audit source covers owners below `o4875`; their corresponding previous-package Relations are already closed. They are **read-only dependencies here** and are not authorized writes.

No replacement Relation ID may be minted for any accepted pair when a stable Relation already exists. Remote writes must be exact endpoint closure only; no unrelated payload on those owners may change.

## Sol identity decisions

### `there@o4972` — pairwise vs three-way confusable identity

Current has two overlapping representations:
- pairwise deep Relation `deep:confusables:there:54e2eb1e8f962f53` for `there → their`, carrying verified boundary/evidence and stable sense anchors;
- horizontal cluster `confusable:horizontal:6f5711efdc5cb02126fb` for the learner boundary `their ↔ there ↔ they're`, whose own learner note explicitly says to present the three-way cluster once.

Frozen decision:
- **survivor Relation ID:** `confusable:horizontal:6f5711efdc5cb02126fb`;
- preserve `there@o4972` and `their@o4963` Word/sense identities;
- migrate/preserve the verified pairwise boundary evidence and stable `there/their` anchors on the survivor representation;
- add reciprocal learner visibility from `their`;
- keep `they're` as the contraction surface `they are` inside the cluster, not as a newly minted Word owner;
- retire/deprecate `deep:confusables:there:54e2eb1e8f962f53` as the competing duplicate representation after its evidence/anchors are preserved;
- create no third Relation identity.

This is stable Relation consolidation, not a Word merge.

### `think@o4986` — ordinary sense restoration + duplicate inflection/family + suppose contrast

Current has two ordinary `think` sense lineages in deprecated/reference state while constructions still depend on ordinary thinking/belief truth:
- `sense:think:1a92a30e99f354f1` — use the mind / consider;
- `sense:think:5f219d551ca75ebb` — believe / hold an opinion.

It also carries competing `think ↔ thought` representations (`rel:v3-family:think:thought`, `rel:v3-family:thought:think`, and horizontal `relation:horizontal:5a161f25cb2d93e234c4`) plus existing deep `think ↔ suppose` Relation `deep:semantic_contrast:think:ee702506c9caa8eb`. Current `suppose` has active target `sense:suppose:45d1373fdfae5328`; old `sense:suppose:08f0eab612235c91` is merged into it.

Frozen decision:
- reactivate/reuse the two ordinary `think` stable senses above; do not mint replacements;
- **survivor `think ↔ thought` representation:** `relation:horizontal:5a161f25cb2d93e234c4`, because the learner boundary must represent both irregular inflection and independent `thought` noun truth;
- preserve `sense:thought:5a8144af086d508f` as the irregular past/past-participle branch and preserve independent noun senses;
- retire/deprecate the two redundant directional `rel:v3-family:*` representations after survivor closure; create no third family Relation;
- preserve `deep:semantic_contrast:think:ee702506c9caa8eb` as the `think ↔ suppose` survivor;
- source-anchor that contrast to the restored ordinary belief/opinion `think` lineage and target-anchor it to active `sense:suppose:45d1373fdfae5328`;
- replace any stale nested payload anchor to merged `sense:suppose:08f0eab612235c91` with the active target and add reciprocal `suppose@o4825` learner visibility;
- do not unmerge the old `suppose` sense and do not mint a new Relation.

### `turkey@o5159` — common noun vs proper-name capitalization identity

Current correctly preserves separate stable semantic branches, including country sense `sense:turkey:8cf50...`, but stores them under one lowercase owner and already carries a case-sensitive `capitalized_proper_lexeme` identity overlay for surface `Turkey`.

Frozen decision:
- preserve **one Word owner** `word:turkey`; do not split or duplicate the Word;
- preserve existing common bird/failure senses and the existing stable country sense;
- preserve/reuse the existing case-sensitive proper-lexeme identity overlay and expose `Turkey` as the capitalized Form/identity surface anchored to the country sense;
- learner Core/ranking for lowercase `turkey` must not collapse the country proper name into ordinary lowercase common-noun truth;
- lookup/projection may resolve `Turkey` to the same Word owner through the proper identity/Form layer; no new semantic branch is justified solely by capitalization.

This is `REUSE_EXISTING_STABLE` with Form/identity cleanup.

### `verse@o5276` — lexicalized `versed` Form vs productive verb lineage

Current active `sense:verse:3766409372fe50a5` models “familiarize / make proficient” and carries `be versed in`, but accepted Audit evidence says modern learner truth is lexicalized/adjectival `versed / well versed`; ordinary modern verb `verse` is centered on poetry/versify.

Frozen decision:
- preserve ordinary active noun/poetry and poetry/versify verb senses of `verse`;
- preserve stable `sense:verse:3766409372fe50a5` for continuity but move it to deprecated/reference-only historical verb truth; do **not** merge it into a poetry sense and do not mint a replacement sense;
- detach `be versed in` / `be well versed in` from presentation as a productive modern `verse sb in` verb;
- encode/reuse same-owner Form/lexicalized-participle truth for surface `versed` / `well versed`, with learner meaning “knowledgeable/skilled/experienced in” and pattern `be (well) versed in sth`;
- do not mint a separate `versed` Word owner merely to solve Form ownership.

This is stable-sense deactivation plus same-owner Form lifecycle repair.

## Other frozen execution boundaries

- `till@o5026`: follow Production's already accepted narrow escalation rule. Preflight Current registry first; reuse a valid stable lineage if one exists. Only if no stable lineage can represent the accepted branch may executor create a genuine `NEW_SEMANTIC_BRANCH`. This is not a reopened Sol risk.
- `therefore@o4975`, `thus@o5017`, `total@o5070`, `uncover@o5181`, `unit@o5205`, `unite@o5206`, `unity@o5207`: close the already accepted Relations reciprocally using existing stable Relation identity; remote endpoints are limited by the whitelist above.
- `university@o5210`: do **not** make the current university→universal modern “word-family” relation reciprocal. Demote/retire that misleading relation representation as directed by Audit.
- All other `FLIP_TO_UPGRADE`, `REFINE_UPGRADE`, and retained Production `UPGRADE` owners use the exact desired state in the accepted Audit/Production handoffs, with Audit overriding narrower Production wording.
- All owners absent from the exact 159-owner compiled source union are byte-preserved relative to the pinned implementation baseline except where deterministic registry/materialization metadata must change as a consequence of an explicitly authorized source mutation.

## Execution constraints

- Stable identity changes only via `REUSE_EXISTING_STABLE`, a genuinely justified `NEW_SEMANTIC_BRANCH`, or the exact four Sol decisions in this file.
- Where Production/Audit says NEW only if no stable branch exists, preflight registry first and reuse stable continuity when present.
- Reciprocal Relation closure reuses existing Relation ID unless this file explicitly names a survivor/retirement decision.
- Same-owner capitalization/Form/participle repair never justifies duplicate Words.
- Mechanical/schema/hash/Git/materialization/CI blockers are executor-owned and must not become semantic re-review.
- Any stale hand-copied stable ID discovered by execution must be mapped only to the exact Current registry identity; stale-ID correction cannot silently create or re-decide semantics.

## Package exit

Before `main` advances from `o4874` to `o5374`, require:
- **500/500** owner readback;
- exact **159/159** semantic source-owner accounting from the compiled artifact;
- exactly the eight authorized out-of-range Word writes above and no others;
- all four Sol identity decisions closed exactly as frozen;
- accepted reciprocal Relation/Form closures complete without duplicate identities;
- Natural Owner registry reconstruction/audit PASS;
- transport guards PASS;
- Lexical shard tests PASS;
- all changed lexical JSON parse PASS;
- full Astro build PASS;
- durable 500-owner package receipt;
- package merge to main;
- only then advance Current frontier `o4874 → o5374`.

`o5375+` remains **NOT_ACTIVATED** except for the single exact `whole@o5416` remote endpoint write authorized above.
