# Lexical Sol Final Reconciliation — o0825–o1024

Status: **RECONCILED / READY_FOR_BOUNDED_CORRECTIVE_IMPLEMENTATION**

Role: single executable semantic authority for the 10 durable Independent-Audit delta owners inside the already mechanically implemented `o0825–o1024` window.

Authority inputs:

- `content/lexical/semantic-audit/o0825-o1024.audit.md` (`HOLD_FOR_SOL`);
- current Natural Owner truth after retro cleanup through `o0824`;
- exact `coach → train` Relation owner and remote `train@o5092` identity readback;
- `CONTENT_ASSET_CONTRACT.md` + `CONTENT_EXECUTION.md`.

PASS owners remain untouched.

## Terminal accounting

```text
Audit delta owners                         10 / 10
  Production NO_CHANGE false-pass           6
  Production UPGRADE refine                 2
  Production UPGRADE identity-risk          2

Final identity actions
  NEW_SEMANTIC_BRANCH                        0
  REACTIVATE_EXISTING_STABLE                 2  (train verb; conquest result noun)
  terminal ESCALATE_IDENTITY                 0

New Relation IDs                            0
Remote Word writes                          1  (train@o5092 only)
```

## Executor rules

1. No new stable sense ID or Relation ID is authorized.
2. Reuse/reactivate exact stable IDs named below.
3. The only out-of-window Word owner mutation is `train@o5092`, solely to restore the existing training-verb stable identity required by the already-existing `coach → train` semantic contrast and train construction.
4. Pronunciation corrections are Form overlays on existing semantic senses, never fake semantic branches.
5. Final acceptance requires exact write-set validation, all 10 delta owners plus `train@o5092` readback, identity/reference closure, Relation/Form closure, Natural Owner audit, JSON/tests, `git diff --check`, and Astro build.

## A. Production `NO_CHANGE` false-pass — 6

### `o0832 classify` — WORD-FAMILY REANCHOR
Preserve `sense:classify:f433e79cedca5900`. Retarget the `classify → class` family edge from student-group `sense:class:af4577a133915bb1` to category/type `sense:class:e306d042096c5b10`. No new relation/sense.

### `o0961 compound` — FORM OVERLAY
Preserve all current noun/adjective/verb senses. Add learner-consumable POS-conditioned pronunciation boundary:

- noun/adjective `compound`: initial stress;
- verb `compound`: second-syllable stress.

Do not split semantic identities for pronunciation.

### `o0965 compress` — FORM OVERLAY
Preserve verb and L3 noun-compress branches. Add:

- noun `compress`: initial stress;
- verb `compress`: second-syllable stress.

No semantic split.

### `o0971 comrade` — ATTACHMENT DEDUP
Keep `comrade in arms` only on companion/fellow-soldier `sense:comrade:65acffc563645347`. Remove the duplicate political-party attachment from `sense:comrade:1267eaa691545d13`. Keep `old comrade` on the political/party branch where appropriate.

### `o0978 concern` — CORE HIERARCHY
Learner Core noun center must include:

- matter/issue needing attention `sense:concern:56c8bb50fbcf5795`;
- worry/anxiety `sense:concern:6416d68100055935`.

Keep low-frequency company/business `sense:concern:13ed4345126650ac` active at L3/Expansion but remove it from default Core. Preserve verb relevance and worry branches.

### `o1012 conquest` — REACTIVATE EXISTING STABLE
Keep act-of-conquering `sense:conquest:871fdbf3ea6c5d26` primary. Reactivate exact historical result/object branch `sense:conquest:9b388a75283d587e` at L2: something gained or won by conquest, including territory or figurative affection. Core may then truthfully say act of conquering / something conquered or won. No new sense.

## B. Production `UPGRADE` identity continuity — 2

### `o0869 coach` + remote `o5092 train` — REACTIVATE TRAINING VERB / REPAIR RELATION
Coach current truth remains:

- coach noun branches;
- coach verb `sense:coach:624256fd61275b94` = train/teach someone.

On exact remote `train@o5092`:

- reactivate `sense:train:5457a182d3a955d7` = teach a person or animal a skill/behavior as an active verb branch;
- clear its erroneous merge into railway noun `sense:train:ea2e5f129cbc5703`;
- keep the railway noun active and separate;
- existing `train sb to do sth` construction already names `5457...`; after reactivation it becomes identity-valid rather than pointing at merged history;
- add the restored training verb into learner Core without opportunistically restoring other merged train branches.

Reuse existing Relation `deep:semantic_contrast:coach:2e2faf9359797f17` and normalize **all** target anchors (top-level, content, stage metadata, Word view) to `sense:train:5457a182d3a955d7`. Do not target railway noun `ea2e...` and do not create another Relation ID.

### `o1000 conflict` — CURRENT IDENTITY ALREADY RESTORED / FORM ONLY
Current integrated truth already has verb `sense:conflict:bbf6c432f6a65e2a` active and separate from noun `sense:conflict:2e2971bdbe0c5c93`; therefore the historical cross-POS merge risk is already resolved in Current and no unmerge/new sense is needed.

Keep `conflict with sth` on the verb and add Form boundary:

- noun `conflict`: initial stress;
- verb `conflict`: second-syllable stress.

## C. Production `UPGRADE` refinements — 2

### `o0905 combine` — FORM OVERLAY
Preserve the single I/T verb semantic identity and existing noun branch. Add pronunciation boundary:

- noun `combine`: initial stress;
- verb `combine`: second-syllable stress.

### `o0930 compact` — FORM OVERLAY
Preserve accepted adjective/verb/current noun branches. Add learner Form truth:

- noun `compact`: initial stress;
- adjective/verb `compact`: second-syllable stress.

Do not promote lower noun meanings merely to justify pronunciation metadata.

## New branch / Relation ledger

```text
new semantic branches: 0
new Relation IDs:       0
```

## Acceptance readback checklist

- exact in-window Word write set: `832,869,905,930,961,965,971,978,1000,1012`;
- exact remote Word write: `5092` only;
- `train:5457...` active and no longer merged to railway noun;
- existing train construction points to active `5457...`;
- coach/train Relation all target anchors resolve to `5457...`;
- classify family target is category/type class `e306...`;
- `comrade in arms` occurs only on companion/fellow-soldier branch;
- concern Core includes worry `6416...` and does not promote company `13ed...`;
- conquest result/object `9b388...` active at lower learner level;
- Form overlays present for combine, compact, compound, compress, conflict with correct POS-conditioned stress boundary;
- no new stable sense or Relation ID generated;
- Natural Owner / identity / Relation / Form closure PASS;
- JSON/tests PASS;
- `git diff --check` PASS;
- Astro build PASS.
