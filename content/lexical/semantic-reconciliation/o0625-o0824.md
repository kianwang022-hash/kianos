# Lexical Sol Final Reconciliation — o0625–o0824

Status: **RECONCILED / READY_FOR_BOUNDED_CORRECTIVE_IMPLEMENTATION**

Role: single executable semantic authority for the 22 durable Independent-Audit delta owners inside the already mechanically implemented `o0625–o0824` window.

Authority inputs:

- `content/lexical/semantic-audit/o0625-o0824.audit.md` (`HOLD_FOR_SOL`);
- `content/lexical/CONTENT_ASSET_CONTRACT.md`;
- `content/lexical/CONTENT_EXECUTION.md`;
- current Natural Owner truth after the closed retro corrections through `o0624`;
- bounded current-delta inventory from Actions run `34932830767`, corrected before reconciliation to replace the accidental PASS read `Christian@o0801` with the actual Audit delta `circle@o0809`.

PASS owners remain untouched. In particular `Christian@o0801` is **not** an implementation target.

## Terminal accounting

```text
Audit delta owners                         22 / 22
  Production NO_CHANGE false-pass          17
  Production UPGRADE refine                 3
  Production UPGRADE identity-risk          2

Final identity actions
  NEW_SEMANTIC_BRANCH                        2
  terminal ESCALATE_IDENTITY                 0

Cross-word Relation actions
  new Relation IDs                           1  (cigaret ↔ cigarette spelling variant)
  remote Word owner writes                   1  (cigarette@o5522 only)

Future activation
  o0825+                         NOT AUTHORIZED BY THIS FILE
```

## Executor rules

1. Apply only the exact desired states below.
2. Generate new stable sense IDs only for the two branches in the New-branch ledger (`bypass` road noun; `cast` actor-selection verb).
3. `campaign` uses its existing stable verb `sense:campaign:c3d1928460d45a5a`; current registry readback already shows that ID active, so no new branch or cross-POS replacement is authorized. Repair only learner attachments/presentation around that stable identity.
4. `cigaret@o0807` and `cigarette@o5522` remain distinct frozen Word owners/stable senses. Reconcile them with one explicit spelling-variant Relation; standard learner presentation is `cigarette`, while `cigaret` is a less-common variant. Do not delete or merge either Word identity.
5. The only out-of-window Word owner write is `cigarette@o5522` for reciprocal spelling identity presentation.
6. All deprecated/reference stable senses named below remain preserved unless this authority explicitly reactivates one.
7. Final acceptance requires exact write-set validation, all 22 delta-owner readback, the exact remote dependency readback, stable identity/reference closure, Relation/Form closure, Natural Owner audit, JSON/tests, `git diff --check`, and Astro build.

## A. Production `NO_CHANGE` false-pass corrections — 17

### `o0629 burst` — ATTACHMENT / CONSTRUCTION CLEANUP
Keep physical rupture `sense:burst:0b33712132985e9d`, sudden movement `sense:burst:e2fb1f06db0d5470`, sudden-emergence `sense:burst:9c9d001b2c715063`, and noun burst separately.

- Remove generic `burst into + sth` from the physical rupture sense.
- Keep `burst into the room` on sudden movement.
- Keep `burst into view` on sudden emergence.
- Expose `burst into tears/laughter` as one word-level high-value construction meaning suddenly begin crying/laughing; it may source the sudden-movement/state transition branch, but must not be taught as physical rupture.
- Remove duplicate broad `burst into + sth` presentation once the specific construction is present.

No new sense.

### `o0633 business` — PRODUCTION STRING
Normalize `it is your business to + do` → `it is your business to do sth` on responsibility `sense:business:9f3cabbbacf55d1a`.

### `o0634 busy` — PRODUCTION STRING
Normalize learner patterns:

- `be busy with + sth` → `be busy with sth`;
- `be busy doing + sth` → `be busy doing sth`;
- `busy oneself with + sth` → `busy oneself with sth`.

No semantic branch change.

### `o0641 buzz` — CORE / REFERENCE BOUNDARY
Keep active buzzing verb `sense:buzz:1b3b510c8ccb59ab`. Remove deprecated/reference `fly low` wording from learner Core. Preserve `sense:buzz:f4ec007996185148` as reference-only history; do not reactivate it.

### `o0643 bypass` — NEW_SEMANTIC_BRANCH
Add one ordinary noun branch: `a bypass` = a road or route built to go around a town, congested area, or obstacle. Keep avoid/go-around verb and surgical bypass separate.

### `o0651 cage` — CORE / REFERENCE BOUNDARY
Remove ice-hockey-goal wording from learner Core. Preserve deprecated/reference `sense:cage:0139693c04c1558c`; do not reactivate it. Keep physical cage, figurative restriction, and cage-verb active.

### `o0664 can` — CORE INTEGRATION
Keep active modal branches:

- ability `sense:can:34096705f0155e9a`;
- permission `sense:can:7705ea8d7787525b`;
- possibility `sense:can:eb46e0117e6159f6`.

Learner Core must organize all three major modal functions, while keeping metal-container noun and can/preserve verb distinct. No new sense.

### `o0670 candy` — CORE / REFERENCE BOUNDARY
Keep noun candy and active transitive sugar-coating/preserving verb `sense:candy:83d97492ef345e08`. Remove deprecated intransitive crystallize-to-sugar wording from learner Core; preserve `sense:candy:ebdf59c5d74c55c2` reference-only.

### `o0677 capacity` — PRODUCTION STRING
Normalize `capacity to do + sth` → `capacity to do sth` on ability `sense:capacity:77abf69d80985f99`.

### `o0689 care` — WORD-FAMILY REANCHOR
Reanchor `care → careful` source from looking-after noun `sense:care:b6f291d45d765516` to caution/attention noun `sense:care:3dd6aea281b4536f`. Target remains `sense:careful:8ae359f4ee0a54c0`.

### `o0697 carrot` — ATTACHMENT
Move `carrot and stick` from literal vegetable `sense:carrot:2645f7201c305a11` to incentive/reward `sense:carrot:76adb5023f4c5f90`.

### `o0706 cast` — NEW_SEMANTIC_BRANCH
Add one common verb branch: select actors for a role/production; expose `cast sb as X` / `be cast as X`. Keep noun actor-cast, throw, mold/casting, plaster cast, and molded-object branches separate.

### `o0748 challenge` — WORD-FAMILY REANCHOR
Keep current challenger family fact but reanchor its source from difficult-task noun `sense:challenge:4cbb0fa790ed5a96` to competition/challenge verb `sense:challenge:58e615d6b7695068`. If the current target sense is unresolved/null because `challenger` is not a current Main Word owner, leave target identity null rather than inventing a stable target. The correction required here is the branch-specific source anchor.

### `o0773 cheese` — CORE / REFERENCE BOUNDARY
Learner Core centers ordinary food `sense:cheese:b5e436c704d35d5d`. Remove both deprecated imperative `cheese it` wording and obscure reel/spool verb wording from default Core. Preserve valid rare verb material in active/reference history at low priority; do not delete stable truth merely because it is L3.

### `o0779 cherish` — ATTACHMENT
Move `cherish a hope` from love/care `sense:cherish:64ca98c6280f5b99` to abstract feeling/idea `sense:cherish:8886cc7c7e5957e0`. `cherish an ambition` remains on the abstract branch.

### `o0809 circle` — CORE / REFERENCE BOUNDARY
Remove theater/opera tier-seat wording from learner Core. Preserve deprecated/reference `sense:circle:08aee532bc4f5aaf`. Keep geometric circle, social circle, surround, and circular-movement branches active.

### `o0814 circus` — REACTIVATE EXISTING STABLE
Reactivate ordinary performance/show `sense:circus:7dc1ec0426505e8c` as a learner secondary branch: a circus performance/show given by acrobats, clowns, and other circus performers. Keep traveling-company `sense:circus:72b26f92d9425480` primary; chaotic-scene figurative branch active; ancient stadium lower. No new sense.

## B. Production `UPGRADE` refinements — 3

### `o0691 careful` — STRING + WORD-FAMILY REANCHOR
Normalize `be careful to do + sth` → `be careful to do sth`. Reanchor `careful → care` target from looking-after `sense:care:b6f291d45d765516` to caution/attention `sense:care:3dd6aea281b4536f`.

### `o0710 cat` — CORE / LOW-REFERENCE CLEANUP
Keep feline `sense:cat:a1e0d53409665e70` as learner Core. Remove deprecated spiteful-woman wording and rare deprecated verb wording from Core. Preserve stable rare/reference senses (`vomit`, cat-o'-nine-tails verb, dated/derogatory noun) as reference-only; do not delete them.

### `o0823 clap` — CORE / REFERENCE BOUNDARY
Keep applaud `sense:clap:354cbc8983ac5791` primary, sound/thunder noun and wings lower. Remove deprecated quick-placement `sense:clap:e337c57aa55d5191` wording from learner Core and preserve it reference-only. Do not reactivate it in this correction.

## C. Production `UPGRADE` identity continuity — 2

### `o0662 campaign` — REUSE_EXISTING_STABLE / ATTACHMENT CLEANUP
Current integrated truth already has verb `sense:campaign:c3d1928460d45a5a` active, so the historical cross-POS merge concern no longer requires a new identity.

- Keep noun campaign branches unchanged.
- Keep/reuse active verb `c3d192...` = organize or take part in sustained campaigning for/against something.
- Move malformed/misattached noun collocation `campaign for + sth` off election-campaign noun `sense:campaign:70b28b1a33c35a36`; the verb owns `campaign for/against sth`.
- Replace the two redundant/incorrectly glossed constructions with one clear learner construction `campaign for/against sth` anchored to the verb.
- Correct verb transitivity/pattern to ordinary intransitive `campaign for/against sth`; do not teach it as a generic transitive verb.

No new sense and no unmerge mutation is needed beyond current registry closure.

### `o0807 cigaret` + remote `o5522 cigarette` — RESOLVED WORD IDENTITY
Preserve both frozen Main Word IDs and their stable noun senses.

Create one `spelling_variant` Relation:

- `cigarette` = standard modern spelling and learner presentation anchor;
- `cigaret` = less-common spelling variant;
- both mean the same tobacco-roll noun.

Keep the existing `cigaret` form overlay and add reciprocal spelling overlay to `cigarette`. Do not delete, merge, or redirect either stable Word identity.

## New-branch ledger — exactly 2

The executor may create exactly these two stable semantic branches and no others:

1. `bypass@o0643` — road/route noun going around a town, congested area, or obstacle;
2. `cast@o0706` — actor-selection verb (`cast sb as X`, select actors for roles).

## New Relation ledger — exactly 1

Create one cross-Word `spelling_variant` Relation `cigaret ↔ cigarette` with reciprocal Word views at `o0807` and remote `o5522`, standard presentation anchor `word:cigarette`, variant `word:cigaret`.

No other new Relation ID is authorized.

## Acceptance readback checklist

- exact delta Word write set is the corrected 22-owner set:
  `629,633,634,641,643,651,662,664,670,677,689,691,697,706,710,748,773,779,807,809,814,823`;
- exact remote Word write is `5522` only;
- `Christian@o0801` remains byte-unchanged;
- exactly two new stable semantic branches, under bypass and cast only;
- exactly one new Relation, cigaret↔cigarette;
- campaign verb `c3d192...` remains active and owns `campaign for/against sth`;
- all Core/reference leaks named above are removed without deleting valid reference identities;
- care↔careful family anchors use the caution/attention branch both directions;
- challenge→challenger source is the competition verb branch;
- `circus:7dc1...` is active again;
- malformed `+ sth` strings named above are absent from learner-facing Current;
- no duplicated generic `burst into + sth` remains beside the specific construction model;
- Natural Owner/identity/reference/Relation closure PASS;
- JSON/tests PASS;
- `git diff --check` PASS;
- Astro build PASS.
