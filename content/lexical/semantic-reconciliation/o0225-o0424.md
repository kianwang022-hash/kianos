# Lexical Sol Final Reconciliation — o0225–o0424

Status: **RECONCILED / READY_FOR_BOUNDED_CORRECTIVE_IMPLEMENTATION**

Role: single executable semantic authority for the 30 durable Independent-Audit delta owners inside the already mechanically implemented `o0225–o0424` window.

Reconciliation reads current truth from branch based on `main@88eedd381aa7c719467c0e925b4147ac31d68537` plus the already-closed earlier correction `o0025–o0224`. PASS owners are not reopened.

Authority inputs:

- `content/lexical/semantic-audit/o0225-o0424.audit.md` (`HOLD_FOR_SOL`);
- `content/lexical/CONTENT_ASSET_CONTRACT.md`;
- `content/lexical/CONTENT_EXECUTION.md`;
- current Word Natural Owners / exact Relation and spelling lookup dependencies;
- bounded current-delta inventory from Actions run `34932077045`.

## Terminal accounting

```text
Audit delta owners                         30 / 30
  Production NO_CHANGE false-pass          17
  Production UPGRADE identity-risk          3
  Production UPGRADE refine/readback       10

Final identity actions
  NEW_SEMANTIC_BRANCH                        2
  terminal ESCALATE_IDENTITY                 0

Form / spelling
  lookup aliases added                       2  (apologize, artifact)
  cross-Word spelling identity               1  (appal ↔ appall)

Cross-window Word dependency
  appall@o6353                               1

Future activation
  o0425+                         NOT AUTHORIZED BY THIS FILE
```

## Executor rules

1. Apply only the exact desired states below.
2. Reuse/reactivate existing stable sense IDs whenever named; generate new sense IDs only for the two `attack` branches in the New-branch ledger.
3. `appal@o0227` and `appall@o6353` remain distinct stable Word owners because ordinals/Word identities are frozen. Reconcile them explicitly as regional spelling variants rather than silently deleting, merging, or redirecting either Word owner.
4. Add `apologize` and `artifact` as deterministic spelling lookup aliases to the existing `apologise` and `artefact` semantic owners; do not create new Word owners.
5. Relation owners are changed only where an Audit finding requires an anchor correction or the one explicit appal/appall spelling relation.
6. The only out-of-window Word owner write is `appall@o6353`.
7. Final acceptance requires exact write-set validation, all 30 delta owner readback, exact dependency readback, stable identity/reference closure, lookup closure, Relation/Form closure, Natural Owner audit, JSON/tests, `git diff --check`, and Astro build.

## A. Production NO_CHANGE false-passes — 17

### `o0225 apologise` — FORM / LOOKUP ALIAS
Keep `word:apologise` and `sense:apologise:c043b63d402e51d6` as the semantic owner. Add a spelling identity overlay: BrE `apologise`, AmE `apologize`. Add lookup spelling `apologize` → the same Word owner / ordinal. Do not create another sense or Word owner.

### `o0227 appal` + remote `o6353 appall` — RESOLVED WORD IDENTITY
Preserve both frozen Word IDs and all stable sense IDs.

Establish an explicit `spelling_variant` Relation with two Word views:

- `appal` = standard British spelling;
- `appall` = standard American spelling;
- semantic meaning is the same verb “shock/horrify greatly”.

`word:appal` is the canonical semantic presentation owner for this reconciliation generation because it is the original in-range Main owner; `word:appall` remains a valid regional-variant owner, not a second competing semantic analysis. Add explicit spelling overlays to the active senses of both owners. Do **not** merge/delete the appall stable senses in this correction.

### `o0232 appearance` — REUSE WORD-FAMILY FACT
Reanchor `appearance → appear` from `sense:appearance:ca7d2898328e5176` (looks) to `sense:appearance:f75603ca08fc56bf` (becoming visible/arriving). Target `sense:appear:570b4973fee75dda` remains the visibility/arrival branch. No new Relation.

### `o0240 application` — REUSE WORD-FAMILY FACT
Reanchor application/use `sense:application:502b9894827c557c` to apply/use `sense:apply:998d31110bdb5f6f`. If the request-family mapping is retained, it must separately use `sense:application:0d3c2d564b735ecc ↔ sense:apply:2816ef9d454a5a15`.

### `o0241 apply` — WORD-FAMILY REPAIR
Remove the learner claim that `appliance` is “the noun form of apply”. Reuse the current family slot for `apply → application` on the use branch (`sense:apply:998d31110bdb5f6f → sense:application:502b9894827c557c`). Add a second bounded branch-specific family fact for request (`sense:apply:2816ef9d454a5a15 → sense:application:0d3c2d564b735ecc`). `appliance` may remain historical/morphological provenance only, not the learner-predictable noun-form boundary.

### `o0243 appointment` — REUSE WORD-FAMILY FACT
Reanchor `appointment → appoint` from meeting `sense:appointment:195081e3ef475bb5` to appointment/assignment `sense:appointment:bdde77f539e35a8d`; target remains `sense:appoint:64330df3583f5018`.

### `o0247 appropriate` — FORM OVERLAY
Preserve all senses under one Word. Add same-spelling POS pronunciation boundary: adjective ending /ət/ vs verb ending /eɪt/. No semantic split.

### `o0262 argument` — REUSE WORD-FAMILY FACT
Retarget `sense:argument:3c548d0cbf2b5c82` (reason/set of reasons) to `sense:argue:bd6c743958815e64` (state reasons / argue a position), not quarrel `sense:argue:96e9bfb7067f51e2`.

### `o0277 artefact` — FORM / LOOKUP ALIAS
Keep `word:artefact` semantic ownership. Add spelling overlay: BrE `artefact`, common AmE/international `artifact`. Add lookup `artifact` → the same owner/ordinal. No new Word/sense.

### `o0280 articulate` — FORM OVERLAY
Add adjective-vs-verb pronunciation boundary: adjective ending /lət/ vs verb ending /leɪt/. Preserve semantic branches.

### `o0298 assemble` — REUSE_EXISTING_STABLE
Keep gather `sense:assemble:d6c415478107512b`; governing pattern must expose both transitive and intransitive use (`assemble a crowd`; `students assembled`). No new branch.

### `o0345 authority` — PRODUCTION STRING
Normalize `authority to do + sth` → `authority to do sth` on `sense:authority:270556ef809557df`.

### `o0376 bad` — WORD-FAMILY REANCHOR
Retarget ordinary `bad → badly` manner relation from severe-degree `sense:badly:d0b3a11fa4905200` to manner `sense:badly:2f3bfb9bad19538e`. Keep severe-degree badly separate.

### `o0378 badly` — CORE INTEGRATION
Core must explicitly organize both ordinary branches: severe degree (`badly damaged / badly needed`) and unsatisfactory manner (`behave badly`). Do not hide either active branch.

### `o0382 bail` — ATTACHMENT REPAIR
Remove `bail out` from noun bail-money `sense:bail:e59ffb08fe49516e`. Legal `bail sb out` belongs to the release verb `sense:bail:155f745949cd5471`; general rescue/financial `bail sb/sth out` remains construction-level. `post bail` stays on the noun.

### `o0399 banner` — ATTACHMENT REPAIR
Keep `a banner year` only on adjective `sense:banner:373e5ce422c55a65`; remove the duplicate noun attachment. `under the banner of` stays noun-owned.

### `o0406 bargain` — ATTACHMENT REPAIR
Remove `bargain with sb` from noun agreement `sense:bargain:ebb1e0f9505f5330`; preserve verb `sense:bargain:57b6f784299e5fc4` and its `bargain with sb for sth` construction once.

## B. Identity continuity — 3

### `o0249 approve` — REACTIVATE / MERGE EXISTING STABLE
Formal authorize `sense:approve:a9fb49704e9c5fab` remains active.

Reactivate `sense:approve:aed379dabd6e5c51` as the learner-active `approve of sb/sth = think well of / consider acceptable` branch. Merge historical near-duplicate `sense:approve:7794863e5d875222` into this restored survivor, **not** into the formal-authorize branch. Re-anchor all `approve of` constructions to the restored `aed379...` sense and deduplicate them to one learner-visible construction.

### `o0268 arouse` — REACTIVATE EXISTING STABLE
Keep reaction/emotion `sense:arouse:0fd24cf9eccd57dd` primary. Reactivate `sense:arouse:4d8ea8ea22f35192` as lower active wake-from-sleep/wake-up branch. Sexual-arousal specialist branch remains reference/deprecated. No new sense.

### `o0309 associate` — REACTIVATE EXISTING STABLE + FORM
`associate with sb` already uses `sense:associate:ec7fa5a747735c15`; reconcile registry status by reactivating that same stable ID. Keep link-A-with-B verb `sense:associate:8597335726c65975` and colleague noun `sense:associate:aaad4a8f591d5300`.

Add noun-vs-verb pronunciation Form boundary: noun `associate` ends /-ət/; verb `associate` ends /-eɪt/. The two verb senses share the verb pronunciation. No new semantic identity.

## C. Production UPGRADE refinements / readback — 10

### `o0254 arch` — CORE CLEANUP
Deprecated `chief/principal` must not remain in active Core. Keep ordinary arch structure/shape, verb arch, and lower mischievous/playful adjective. Do not reactivate chief/principal.

### `o0267 around` — CORE INTEGRATION
Integrate active `sense:around:1cfe2000dfc853bd` = approximately/about into Core/Recall model alongside circular/nearby uses.

### `o0273 arrive` — CONSTRUCTION SEMANTICS
Keep physical arrival and success/“has arrived” branches separate. Remove `arrive at + sth` from the success/fame branch. Preserve a distinct word-level construction `arrive at a conclusion/decision/figure` = reach a result/conclusion; it must not be defined as “become successful”. No new stable branch is required.

### `o0296 assassinate` — RANKING/CORE
Keep murder of a prominent/public person `sense:assassinate:3d5f697679e15f74` as the active center. Figurative reputation-damage stable sense remains reference/deprecated and must disappear from Core.

### `o0312 assumption` — ATTACHMENT
Move `assumption of innocence` away from pretense `sense:assumption:7c27e3aef13b5277`; attach it to ordinary assumption/presumption `sense:assumption:fa2acd3fe26b58c4`. Learner gloss: legal presumption of innocence, not pretending to be innocent.

### `o0313 assurance` — ATTACHMENT
Keep `with assurance` only on confidence `sense:assurance:52446fbf3aac5479`; remove duplicate/misattached use from guarantee `sense:assurance:107165f67eb55373`.

### `o0320 atmosphere` — RESOLVED STABLE MERGE / DEMOTION
Use `sense:atmosphere:a205b0256dc5591b` as the single active physical gaseous-envelope branch and broaden it to Earth **or another celestial body**. Merge/deprecate duplicate celestial-body `sense:atmosphere:b9b5a2dabb1056df` into that survivor. Demote/deprecate weak `weather/climate` `sense:atmosphere:28a2919be0dc5516` to reference. Keep social/mood atmosphere `sense:atmosphere:1ba9fbb729975837` active.

### `o0323 attack` — 2 × NEW_SEMANTIC_BRANCH
Preserve physical attack verb/noun. Add exactly two learner-worthy branches:

1. **strong criticism** — verb/noun truth for attacking a policy/argument/person verbally or in writing;
2. **illness episode** — noun `an attack of asthma/illness` = a sudden episode.

Keep the existing attack↔assault Relation attached to a compatible attack branch; do not anchor it to illness. Its learner boundary must remain sense-grounded rather than pretending assault is interchangeable in every figurative context.

### `o0333 attribute` — REUSE EXISTING RELATION
Reuse `deep:semantic_contrast:attribute:3d20a99fb2761260`; reanchor source everywhere from noun `sense:attribute:ae2dae6afe725db5` to active attribution verb `sense:attribute:9d75480caee85cd9`. Target remains `sense:ascribe:85cde079b0755b71`. Existing pronunciation Form truth remains unchanged.

### `o0383 bait` — REACTIVATE EXISTING STABLE / BROADEN
Reuse historical torment/provoke branch `sense:bait:2cd01fa3dd2c537b` rather than minting a new identity. Reactivate it as lower active modern `provoke/taunt/deliberately annoy` truth, preserving historical torment continuity in reference/provenance. Literal lure-with-bait verb remains separate.

## New-branch ledger — exactly 2

The executor may create exactly these two stable branches and no others:

1. `attack@o0323` — strong criticism verb/noun learner branch (one semantic branch may be represented with the POS needed by the repository's current sense model; do not proliferate duplicates);
2. `attack@o0323` — sudden illness-episode noun branch.

## New Relation ledger — exactly 1

Create one `spelling_variant` Relation for `appal ↔ appall` with Word views at `o0227` and remote `o6353`. The Relation explicitly records BrE/AmE regional spelling equivalence and designates `word:appal` as the current semantic presentation anchor without deleting `word:appall`.

No other new Relation ID is authorized.

## Lookup mutations — exactly 2 new spellings

- `apologize` → existing `word:apologise`, ordinal `225`;
- `artifact` → existing `word:artefact`, ordinal `277`.

Keep existing `apologise`, `artefact`, `appal`, and `appall` lookup entries.

## Acceptance readback checklist

- all 30 delta owners read back against this authority;
- only exact remote Word owner `o6353 appall` changes outside the window;
- exactly two new semantic branches, both under `attack@o0323`;
- exactly one new Relation, `appal ↔ appall`;
- exactly two new lookup spelling aliases, `apologize` and `artifact`;
- `approve of` uses reactivated `aed379...`; `779486...` is merged into that survivor;
- `arouse` wake branch `4d8...` active; `associate ec7...` active;
- appearance/application/appointment/argument/bad family anchors match the named senses;
- attribute/ascribe Relation sources attribution verb `9d754...`;
- no malformed `authority to do + sth` remains;
- no noun-attached `bail out`, `banner year`, or `bargain with sb` duplicate remains;
- Core for arch/around/assassinate/atmosphere/badly reflects the reconciled active model;
- attack criticism/illness branches are active and the assault contrast remains compatible;
- changed lookup JSON resolves both added variant spellings to the intended existing Word owners;
- Natural Owner/identity/reference/Relation/lookup closure PASS;
- lexical JSON/tests PASS;
- `git diff --check` PASS;
- Astro build PASS.
