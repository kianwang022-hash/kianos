# Lexical Semantic Review — o1125–o1374

Status: **SEMANTIC REVIEW COMPLETE / IMPLEMENTATION NOT YET APPLIED**

Role: non-canonical editorial handoff from ChatGPT/Sol semantic review to Codex/Luna repository execution.

This file records the fresh semantic decision for exactly 250 Current Word owners (`o1125–o1374`) against lexical Current Truth read from pinned semantic baseline `main@262ba0755b212215efa0a3840ff0664430157fd2`.

At handoff time, current main had advanced to `c7e5bb6edcece2d64055d90acd48d3fee3dda420`; comparison from the pinned baseline showed only semantic-audit-router / audit-manifest and unrelated Politics changes, with no lexical Word-owner drift in this scope. Therefore the reviewed owner judgments remain valid.

This handoff does **not** mutate canonical lexical truth, reactivate Catalog execution, grant learner K/P/R/E/U acceptance, authorize `o1375+`, or replace final integrated readback.

## Governance boundary

- Semantic quality authority: `content/lexical/CONTENT_ASSET_CONTRACT.md`.
- Independent audit governance: `content/lexical/INDEPENDENT_SEMANTIC_AUDIT_CONTRACT.md`.
- Machine audit-routing spec: `content/lexical/SEMANTIC_AUDIT_RISK_ROUTER_SPEC.md`.
- Independent Audit Lane: GitHub Issue #106.
- Production-side reverse-check below is a self-check only; it is **not** the independent Audit Pack.
- The semantic operation decision in this handoff is complete: each ordinal is exactly one of `NO_CHANGE` or `UPGRADE`; `BLOCKED=0`.
- `SAFE_SIMPLE` / `DEPTH_READY` learner-content routing is not an instruction to mutate semantic identity. Final quality/risk routing may be refined by the strict independent Audit + Sol reconciliation without changing a correct `NO_CHANGE`/`UPGRADE` semantic target.

## Frozen implementation rules

For any later authorized implementation:

- Default mechanical executor: Luna/Codex.
- `NO_CHANGE` means preserve semantic content and byte-preserve the Current baseline owner/dependencies when hashes still match.
- `UPGRADE` means implement only the bounded semantic target below; no opportunistic cleanup.
- Stable identity rule from PR #95 remains frozen:
  - `REUSE_EXISTING_STABLE` when semantic continuity exists;
  - `NEW_SEMANTIC_BRANCH` only for a genuinely new branch;
  - `ESCALATE_IDENTITY` for genuine split/merge/Form/Relation identity ambiguity.
- Rewording, broadening, narrowing, correcting, re-leveling, or reactivating the same branch does not justify a replacement sense ID.
- A deprecated stable sense may be reactivated when this review restores the same semantic branch.
- Do not deprecate an active main sense merely to make wording cleaner.
- Relation / word-family / exam-mapping / Form references must close against final identity.
- Engineering/schema/hash/Git/CI problems remain executor-owned; semantic ambiguity returns narrowly to Sol.

## Summary

- Scope: **250 owners**
- `NO_CHANGE`: **183**
- `UPGRADE`: **67**
- `BLOCKED`: **0**
- Checkpoints:
  - `o1125–o1174`: 35 `NO_CHANGE` / 15 `UPGRADE`
  - `o1175–o1224`: 34 `NO_CHANGE` / 16 `UPGRADE`
  - `o1225–o1274`: 42 `NO_CHANGE` / 8 `UPGRADE`
  - `o1275–o1324`: 35 `NO_CHANGE` / 15 `UPGRADE`
  - `o1325–o1374`: 37 `NO_CHANGE` / 13 `UPGRADE`
- Production-side reverse-check re-read complex `NO_CHANGE` examples including `depth`, `derive`, `descent`, `describe`, `description`, `desire`, `despair`, `desperate`, `detect`, `develop`, `diagnose`, `dictate`, `die`, `different`, and `difficulty`; no operation flip was required.
- Over-upgrade reverse-check re-read borderline upgrades including `despatch`, `determine`, `devil`, `dialog`, `diet`, and `differentiate`; all remained justified.

## Exact operation coverage

Each ordinal in `o1125–o1374` appears exactly once below, either in an exact `NO_CHANGE` list or as an `UPGRADE` entry.

### Checkpoint 1 — o1125–o1174

`NO_CHANGE`:

`o1125,o1126,o1127,o1128,o1129,o1130,o1131,o1132,o1135,o1138,o1140,o1141,o1142,o1143,o1144,o1145,o1146,o1148,o1149,o1151,o1152,o1153,o1156,o1159,o1160,o1162,o1164,o1166,o1167,o1168,o1169,o1170,o1172,o1173,o1174`

`UPGRADE`:

- o1133 **cover**
  - Preserve existing stable branches, but integrate learner-worthy `cover costs/expenses` = pay or be sufficient for costs and `cover a story/event` = report on it into the active/Core organization where they are currently underrepresented.
  - Keep literal covering/protecting/concealing branches primary; do not invent duplicate identities for existing stable senses.

- o1134 **cow**
  - Restore/reuse the ordinary verb branch `cow sb` = intimidate/frighten into submission, which the Core/construction already presupposes but the active sense layer does not close.
  - Surface `cow sb into doing sth` as the reusable construction.

- o1136 **crab**
  - Recompress learner Core around the ordinary animal meaning.
  - Do not let low-value grumpy-person / complaint / rowing or other fringe material define the Core; preserve valid low/reference branches rather than deleting them merely because they are low priority.

- o1137 **crack**
  - Add/reuse the high-value figurative verb branch `crack a code/case/problem` = solve or decipher something difficult.
  - Preserve existing physical break/split and noun branches; do not collapse the figurative solving branch into physical cracking.

- o1139 **craft**
  - Add/reuse ordinary verb `craft sth` = make, shape, or design carefully/skillfully, including abstract products such as an argument/policy/message where appropriate.
  - Keep noun craft/skill/handicraft branches; do not let the low-value aircraft/boat-related material dominate learner Core.

- o1147 **creature**
  - Restore/reuse the ordinary noun branch `creature` = living being/animal, which Core expresses but active coverage does not adequately close.
  - Keep figurative/person-use lower where valid.

- o1150 **creep**
  - Correct the learner-facing noun payload: `a creep` = an unpleasant/objectionable person; do not teach `a creepy person` as though `creepy` were the noun form.
  - Preserve verb `creep` movement branches and keep `creepy` as separate word-family/Form truth if present elsewhere.

- o1154 **criminal**
  - Restore/reuse the adjective branch `criminal` = relating to crime / illegal (`criminal law`, `criminal activity`), not only noun `a criminal`.
  - Keep noun and adjective distinct without unnecessary identity replacement.

- o1155 **cripple**
  - Add/reuse the figurative verb branch = severely damage, disable, or make a system/economy/organization unable to function normally (`cripple the economy/system`).
  - Do not teach the dated person noun as neutral production vocabulary; demote it from learner Core while preserving valid historical/reference truth.

- o1157 **crisp**
  - Add the familiar figurative adjective branch = clear, sharp, concise, or well-defined (`crisp image`, `crisp answer/style`) where learner-worthy.
  - Preserve literal texture/freshness branches separately.

- o1158 **criterion**
  - Add explicit Form/number boundary: singular `criterion`, plural `criteria`.
  - Do not create separate semantic identities merely for the plural form.

- o1161 **criticise**
  - Add spelling/Form boundary: `criticise` is standard BrE; `criticize` is the common AmE spelling and is also accepted in BrE usage.
  - Same lexical semantics; no duplicate sense tree.

- o1163 **crop**
  - Restore/reuse the verb branch behind `crop up` = appear/arise unexpectedly, because the construction/Core evidence exists but active-sense closure is missing.
  - Keep agricultural/noun branches primary for literal use.

- o1165 **crow**
  - Add/reuse verb `crow about/over sth` = boast or gloat triumphantly.
  - Preserve bird and bird-call branches; register-mark the boast use as appropriate.

- o1171 **cruise**
  - Restore/reuse ordinary pleasure-travel-by-ship branch (`go on a cruise`, `cruise around/along`, ship cruising) where Core currently acknowledges it but active closure is incomplete.
  - Preserve other motion/speed senses without crowding Core.

### Checkpoint 2 — o1175–o1224

`NO_CHANGE`:

`o1175,o1176,o1177,o1178,o1181,o1182,o1183,o1186,o1188,o1189,o1190,o1191,o1192,o1193,o1195,o1196,o1197,o1198,o1199,o1200,o1203,o1204,o1205,o1206,o1209,o1210,o1211,o1212,o1213,o1214,o1217,o1220,o1222,o1223`

`UPGRADE`:

- o1179 **culminate**
  - Restore/reuse active verb `culminate` = reach the highest/final point or result.
  - Surface `culminate in sth` and, where valid, `culminate at` for a peak/point; Core currently has the idea while active senses are empty.

- o1180 **culprit**
  - Add familiar figurative noun = the thing/factor responsible for a problem (`the main culprit behind...`), not only a guilty person.

- o1184 **cunning**
  - Keep deceptive/sly cleverness as the principal learner meaning and preserve genuinely useful skillful/ingenious uses if supported.
  - Remove or demote unsupported `cute/attractive` material from Core rather than teaching it as an ordinary current branch.

- o1185 **cup**
  - Restore/reuse the ordinary drinking-vessel noun branch, which Core presupposes but active sense coverage is incomplete.
  - Keep trophy/unit/other valid branches appropriately ranked.

- o1187 **curb**
  - Restore/reuse ordinary road-edge noun `curb` (AmE; BrE usually `kerb`) and represent the regional spelling/meaning boundary if Form infrastructure permits.
  - Keep restraint/horse-tack/verb `curb` branches distinct.

- o1194 **curriculum**
  - Add number/Form boundary: singular `curriculum`, traditional plural `curricula`; `curriculums` is also used and may be noted as an accepted regular plural where the Form layer supports it.

- o1201 **customer**
  - Repair the word-family anchor `customer → custom`: it must not target `custom` = social tradition/habit merely because spelling/history is related.
  - If Current `custom` has a legitimate stable business/patronage branch, anchor to that branch; otherwise demote/remove this relation from active learner presentation rather than preserve a false semantic anchor. Escalate only if stable relation identity is genuinely ambiguous.

- o1202 **cut**
  - Integrate the already-active noun `cut` = reduction/decrease into Core/learner hierarchy instead of leaving Core blind to it.
  - Add high-transfer phraseology/constructions `cut off`, `cut down on`, and `cut back (on)` with their ordinary reduction/separation meanings; avoid turning the owner into an exhaustive phrasal-verb catalog.

- o1207 **dairy**
  - Add/reuse adjective/general branch `dairy` = relating to milk and milk products (`dairy products`, `dairy industry`), not only noun farm/business.

- o1208 **dam**
  - Restore/reuse ordinary noun `dam` = barrier across water and verb `dam` = block/restrain flow; Core already contains these while active layer is empty.

- o1215 **dare**
  - Represent the modal/semi-modal boundary cleanly: `dare not`, `How dare you...?`, and bare-infinitive behavior where appropriate alongside ordinary `dare to do sth` / `dare sb to do sth`.
  - Remove duplicate presentation rather than inventing duplicate senses.

- o1216 **dark**
  - Add/reuse the familiar figurative branch = sinister, grim, gloomy, troubling, or morally negative (`dark period`, `dark side`, `dark mood`) where learner-worthy.
  - Preserve literal absence-of-light and color branches.

- o1218 **dash**
  - Restore/reuse ordinary active branches: rapid movement/rush (`dash across/into`), punctuation `dash`, and learner-worthy quantity phrase `a dash of sth` = a small amount.
  - Do not let an empty active layer survive while Core carries these meanings.

- o1219 **data**
  - Add usage/Form boundary: traditional plural agreement (`data are`) versus very common modern mass/singular collective usage (`data is`), with register/style context rather than declaring one universally wrong.

- o1221 **date**
  - Add high-transfer historical-origin construction `date from / date back to` = originate from a time in the past.

- o1224 **dawn**
  - Restore/reuse figurative realization branch behind `dawn on sb` = become clear/understood by someone; the construction must not point to an absent semantic branch.

### Checkpoint 3 — o1225–o1274

`NO_CHANGE`:

`o1225,o1226,o1227,o1228,o1229,o1230,o1231,o1233,o1234,o1235,o1236,o1237,o1238,o1239,o1240,o1241,o1242,o1243,o1244,o1245,o1246,o1247,o1249,o1250,o1251,o1252,o1254,o1255,o1256,o1257,o1260,o1261,o1263,o1265,o1266,o1267,o1269,o1270,o1271,o1272,o1273,o1274`

`UPGRADE`:

- o1232 **deal**
  - Restore/reuse ordinary verb `deal with sth/sb` = handle, address, or concern; include `deal in sth` only as a lower business/trading construction where valid.
  - Restore/reuse noun phrase `a great deal (of)` = a large amount/degree.
  - Preserve card-dealing/trade/transaction branches without letting them displace these high-transfer uses.

- o1248 **decision**
  - Restore/reuse ordinary noun `decision` = a choice/conclusion reached after consideration (`make/reach a decision`, `decision on/about sth`, `decision to do sth`).
  - Keep `decisiveness`/ability-to-decide as a separate lower branch if valid; it cannot be the sole active sense.

- o1253 **decline**
  - Correct malformed production string `decline to do + sth` → `decline to do sth`.
  - Preserve existing decrease/refuse/noun branches; no new sense is required merely for this string repair.

- o1258 **deduce**
  - Restore/reuse ordinary verb `deduce` = infer/reach a conclusion from evidence, with `deduce sth from sth` where appropriate; Core/construction exists while active sense closure is empty.

- o1259 **deduct**
  - Restore/reuse ordinary verb `deduct` = subtract/take an amount away, including `deduct A from B`; Core exists while active sense closure is empty.

- o1262 **deep**
  - Add/broaden the familiar intellectual/abstract branch = profound, thorough, or involving serious understanding/thought (`deep understanding`, `deep thought`, `deep analysis`) rather than forcing all such uses into literal depth or generic intensity.

- o1264 **default**
  - Reactivate/reuse the ordinary modern preset/default-setting branch currently deprecated (`default setting/value/option`, `by default`).
  - Keep failure-to-pay/appear and other legal/financial branches distinct; do not create a replacement identity for the same preset branch.

- o1268 **defense**
  - Add explicit spelling/Form boundary: `defense` is standard AmE; `defence` is standard BrE. Same lexical semantics; a hidden search alias alone is insufficient learner-facing Form guidance.

### Checkpoint 4 — o1275–o1324

`NO_CHANGE`:

`o1275,o1276,o1277,o1278,o1280,o1281,o1284,o1288,o1289,o1290,o1291,o1292,o1293,o1294,o1296,o1297,o1298,o1300,o1302,o1303,o1304,o1305,o1306,o1307,o1308,o1310,o1311,o1312,o1313,o1314,o1315,o1317,o1318,o1319,o1324`

`UPGRADE`:

- o1279 **delegate**
  - Add Form/pronunciation boundary between noun `delegate` and verb `delegate` (ordinary English noun ending unstressed /-gət/ vs verb /-geɪt/, locale-appropriate notation).
  - Do not manufacture a semantic split beyond existing noun/verb identities.

- o1282 **delicate**
  - Restore/reuse learner-worthy branches already implied by Core: fragile/easily damaged, subtle/sensitive/requiring tact, in addition to fine/refined structure.
  - Re-rank so Core is not broader than active semantic truth.

- o1283 **delicious**
  - Restore/reuse the ordinary adjective `delicious` = very pleasant in taste/smell; Core exists while active layer is empty.

- o1285 **deliver**
  - Add/reuse childbirth branch (`deliver a baby`; where subject is mother, `be delivered of` is not a modern default production target).
  - Add high-value phraseology `deliver on a promise/commitment` = fulfill what was promised.
  - Preserve delivery/speech/performance branches and stable identities.

- o1286 **delivery**
  - Reactivate/reuse ordinary noun `delivery` = taking/sending goods, messages, or items to a destination; it is currently wrongly deprecated while childbirth remains active.
  - Keep childbirth and speech/manner branches distinct as supported.

- o1287 **demand**
  - Add/reuse verb branch `demand` = require/need as a condition (`the task demands skill`, `the job demands patience`), distinct from explicitly asking insistently for something.

- o1295 **dense**
  - Preserve physical/material density as the main branch.
  - Restore the informal figurative branch `dense` = slow to understand/stupid as a separate low/register-marked semantic branch if supported by its prior stable identity; do not leave it semantically merged into physical density.
  - Because historical identity was merged, executor must reuse/reactivate a valid stable branch if continuity is clear and escalate only if identity cannot be reconciled mechanically.

- o1299 **deny**
  - Add reusable production constructions `deny doing sth` and `deny that + clause` for rejecting an allegation/fact.
  - Preserve `deny sb sth` / refuse-access branches separately.

- o1301 **department**
  - Reactivate/reuse ordinary organizational division/section noun (`sales department`, `department of...`), currently deprecated while an administrative-territory branch remains active.

- o1309 **deposit**
  - Add/reuse noun branch = a layer/accumulation of natural material (`mineral deposit`, `sediment deposits`) in addition to money/payment branches and deposit verbs.

- o1316 **descendant**
  - Restore/reuse ordinary noun `descendant` = a person/organism descended from an ancestor; Core is correct while active senses are empty.

- o1320 **desert**
  - Add explicit Form/pronunciation boundary for noun `desert` (arid land) versus verb `desert` (abandon), and preserve the `desert ↔ dessert` spelling/pronunciation confusable Relation.
  - Teach `just deserts` as the fixed phrase meaning deserved consequences, with its dessert-like pronunciation; do not fake this as the ordinary arid-land sense.

- o1321 **deserve**
  - Add high-value construction `deserve to do/be sth` / `deserve to be done` as appropriate; existing object-taking branch remains.

- o1322 **design**
  - Add purpose constructions `be designed to do sth` and `be designed for sth/sb`; these carry high reading/production value beyond the ordinary noun/verb design senses.

- o1323 **designate**
  - Restore/reuse ordinary verb `designate` = officially choose/name/mark for a role or purpose; Core/construction exists while active layer is empty.
  - Correct malformed `designate sb to do + sth` → `designate sb to do sth`; also preserve `designate A as B` where supported.

### Checkpoint 5 — o1325–o1374

`NO_CHANGE`:

`o1325,o1326,o1328,o1330,o1331,o1332,o1333,o1334,o1335,o1336,o1337,o1338,o1341,o1342,o1343,o1344,o1345,o1348,o1353,o1354,o1355,o1356,o1357,o1358,o1359,o1361,o1362,o1363,o1364,o1365,o1366,o1367,o1369,o1370,o1371,o1373,o1374`

`UPGRADE`:

- o1327 **desolate**
  - Repair Core/label alignment: the ordinary landscape/place branch is `empty, bleak, deserted` while the emotional branch is `extremely lonely/grief-stricken`.
  - Current Core English/sense-label residue incorrectly describes the landscape branch as `crushed by grief`; preserve the two active stable branches but align Core and learner-facing labels to their actual meanings.

- o1329 **despatch**
  - Add spelling/Form boundary that `despatch` is an accepted, chiefly British variant of the much more common modern spelling `dispatch`.
  - Same lexical semantics; do not duplicate the sense tree. Keep low-value historical killing sense reference-only unless independent evidence justifies otherwise.

- o1339 **detach**
  - Restore/reuse ordinary verb `detach` = separate/remove from something, with `detach A from B` / `become detached from` where learner-worthy; Core is correct while active layer is empty.

- o1340 **detail**
  - Restore/reuse the common verb `detail sth` = describe/give the facts or particulars fully (`the report details...`), which Core mentions but active verb coverage currently represents only technical drawing.
  - Preserve noun `detail/details` and technical drawing use separately.

- o1346 **deteriorate**
  - Restore/reuse the ordinary intransitive branch `deteriorate` = become worse (`conditions/health/relations deteriorated`), which Core expresses while active coverage currently contains only transitive `make worse`.
  - Keep transitive use lower where valid.

- o1347 **determine**
  - Reactivate/reuse the stable ordinary branch `determine` = decide/settle something officially or firmly; it is currently deprecated despite Core being organized around `decide`.
  - Keep `determine` = ascertain/discover and = influence/shape as separate active branches.

- o1349 **deviate**
  - Reactivate/reuse ordinary intransitive `deviate from sth` = depart from an established route, norm, standard, or expected course; it is currently deprecated while only transitive `cause to deviate` is active.

- o1350 **device**
  - Restore/reuse ordinary noun `device` = piece of equipment made for a purpose and learner-worthy figurative `device` = method/means/strategy; these stable main branches are currently all deprecated while Core still depends on them.
  - Keep decorative-pattern sense low/reference.

- o1351 **devil**
  - Repair phrase attachment: `a devil of a problem/time/job` is idiomatic emphasis meaning a very difficult/bad example, not evidence for the `cruel/wicked person` sense.
  - Represent it as phraseology/idiomatic use without inventing an unnecessary full sense; preserve supernatural and person branches.

- o1352 **devise**
  - Reactivate/reuse ordinary verb `devise` = think of/invent a plan, method, or idea; active senses are currently empty.
  - If the legal `devise` branch is retained, describe it accurately as verb `give property by will` and keep it low/reference; current Core wording `a legal gift of property` is POS-misaligned.
  - Preserve existing `device ↔ devise` contrast Relation if semantically correct.

- o1360 **dialog**
  - Add spelling/Form boundary with `dialogue`: same core lexical meaning; `dialogue` is the traditional/general spelling, while `dialog` is a common variant, especially in computing and in AmE contexts.
  - Do not duplicate senses solely for spelling.

- o1368 **diet**
  - Add capitalization/identity boundary: ordinary lowercase `diet` = food/eating plan; institutional `Diet` (e.g. the Japanese National Diet) is capitalized in proper-name use.
  - Do not let the legislative branch crowd the ordinary food Core; preserve it as low/reference/Form-aware truth.

- o1372 **differentiate**
  - Repair learner-facing construction to `differentiate between A and B` (not vague/unsafe `differentiate between + sth`) and preserve `differentiate A from B`.
  - Existing distinguish and become-different semantic branches may remain; this upgrade is primarily production/construction cleanup, not a request for a new sense.

## Independent-audit handoff

This package must enter the current STRICT Independent Audit Lane before any audit-supported scale-up claim.

The Audit Chat should use the current machine Risk Router on this exact range and current owner hashes, then follow `INDEPENDENT_SEMANTIC_AUDIT_CONTRACT.md`:

- production `UPGRADE`: 100% reverse-review;
- machine/semantic complex `NO_CHANGE`: STRICT 100% review;
- Form/identity and Relation/anchor/Core↔sense mandatory strata: 100%;
- true simple `NO_CHANGE`: 100% fast gate + deterministic deep sample;
- blind-first owner judgment before reading detailed production rationale;
- report denominators/rates/severity and contract/router SHAs.

Audit corrections are delta evidence; Sol remains final semantic reconciliation authority. Audit must not directly mutate this handoff or canonical content.

## Completion boundary

This file closes **production semantic review only** for `o1125–o1374`.

Do not infer from it that:

- canonical implementation is active;
- `CURRENT.md` should advance automatically;
- `o1375+` is authorized;
- learner K/P/R/E/U changed;
- a green validator or machine Risk Manifest is semantic acceptance.
