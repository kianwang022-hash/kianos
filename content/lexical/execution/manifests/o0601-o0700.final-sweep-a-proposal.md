# Lexical Final-Sweep Production A — o0601–o0700

Status: **POST_FRESH_READ_POST_SELF_ATTACK_BOUNDED_DELTA_GATE**
Semantic standard: `content/lexical/audit/history/2026-09-final-catalog/FINAL_SEMANTIC_FREEZE.md`
Production lane: 词义A
Candidate: `BF06`
Frozen review base: `main@887076790cada8569a772451ab19366ec76f23d5`
Review request commit: `10c05dc259add2a6ab2948ef842eac54bafa0bd5`
Review workflow: `Lexical Review Bundle Export #35466297135`
Review artifact: `10591716235`
Artifact digest: `sha256:a22e8e58be558af0de8a185ae12a177b06d6946775e84395a9ccb1c0f7db0370`
Review coverage: **100 / 100 owners, o0601–o0700**
Self-Adversarial Attack: **COMPLETE**
Semantic BLOCKED: **0**

## Result

- 100/100 owners fresh-read under the frozen final ruler.
- 9 in-range owners need bounded correction inside already-approved learner intent.
- 7 owners contain genuinely new material learner scope.
- 86 owners remain NO_CHANGE after Fresh Read + Self Attack.
- Existing canonical Repair Test blueprints: **43**.
- Final standing Test proposal: **18**.
- New standing Repair Tests created merely because Content was admitted: **0**.
- No canonical semantic mutation is authorized in current `PRODUCTION_SWEEP_ONLY` mode.
- No FLOB rebuild / B audit / semantic materialization is performed in this phase.

The arithmetic counts an owner once even when it has both an internal correction and a genuinely new delta (o0656 call, o0666 cancel).

## A. Bounded corrections — no new Human scope

These repair owner placement, lifecycle, reciprocal Relation integrity, learner-facing correctness, or Test inflation already inside accepted learner intent.

1. **o0607 bubble**
   - Preserve physical bubble, illusion, economic/asset bubble and the ordinary intransitive verb `bubble / bubble up`.
   - Move the low-leverage L3 transitive branch “cause to bubble / bubble out words” out of default Study into Reference.
   - No new meaning.

2. **o0628 burn**
   - The current learner content already visibly owns `burned/burnt` in `be burned/burnt out`.
   - Move this accepted variation into canonical Form/Identity so `burn → burned/burnt → burned/burnt` is represented once in the correct layer.
   - This is structured owner repair, not new learner scope.

3. **o0641 buzz**
   - Correct the Chinese learner gloss for `the buzz about the new movie`.
   - It means excited/interested talk, attention or lively discussion around the movie, not simply “传闻”.
   - Preserve the existing “热议/兴奋活跃的气氛/关注度” Sense.

4. **o0642 by ↔ o0640 buy**
   - Preserve the accepted exact-homophone confusable: `by` = preposition/adverb; `buy` = purchase/believe verb.
   - Make the existing Relation reciprocal from the `buy` side rather than source-only.

5. **o0648 cable**
   - Keep modern default branches: electrical/data cable, strong wire/rope, and current `cable TV` phraseology.
   - Move the telegraph-era verb “send a message by cable” and noun “a telegram sent abroad” out of default Study into Reference.
   - Remove those dated branches from Core/Decision while preserving canonical truth.

6. **o0656 call**
   - Replace the malformed Construction object `vi. + for` with the stable learner pattern `call for sth` = require/demand.
   - Preserve current `call on/upon sb to do sth` and `call sth off` structures.
   - New `call sth into question` scope is separately listed in Section B.

7. **o0662 campaign ↔ effort**
   - Preserve the accepted distinction: a campaign is an organized sustained series of actions toward a goal; effort is broader expenditure of work/energy.
   - Make the current shared Relation reciprocal from the `effort` side rather than source-only.

8. **o0666 cancel**
   - Repair lifecycle/owner placement for `cancel out`.
   - The current phrase already means offset/neutralize, while the underlying offset senses were deprecated and the phrase is attached to the generic “invalidate” branch.
   - Restore/coalesce the modern offset/neutralize branch and attach `cancel out` to it; keep postal/check cancellation as Reference where appropriate.
   - Regional spelling/inflection is separately listed in Section B.

9. **o0674 canvas ↔ canvass**
   - Preserve the high-value spelling/semantic confusable:
     `canvas` = strong cloth/painting surface;
     `canvass` = solicit votes/opinions or examine through discussion.
   - Make the existing Relation reciprocal from the `canvass` side rather than source-only.

## B. Genuinely new final-standard delta — requires bounded Kian approval

These seven owners survived UNDER-UPGRADE and OVER-UPGRADE attacks.

1. **o0614 build**
   - Add Form/Identity `build → built → built`.
   - Add the high-value academic/productive Construction `build on/upon sth`: use an existing result, idea, achievement or evidence as a basis for further development.
   - Keep `build up` as the already-owned separate Construction.
   - No new standing Repair Test.

2. **o0629 burst**
   - Add irregular Form/Identity `burst → burst → burst`.
   - No new semantic branch and no standing Test.

3. **o0640 buy**
   - Add irregular Form/Identity `buy → bought → bought`.
   - No new standing Test.

4. **o0656 call**
   - Add the formal high-value phrase `call sth into question`: cause doubt about the truth, validity, reliability or legitimacy of something.
   - Academic/argumentative Expansion / phraseology; no standing Test.

5. **o0666 cancel**
   - Add regional Form/Identity:
     - mainly UK: `cancelling / cancelled`;
     - US usually: `canceling / canceled`.
   - Same lexeme, spelling/inflection boundary only; no standing Test.

6. **o0679 capital**
   - Add the familiar-new adjective branch “punishable by death / involving the death penalty”.
   - Anchor with `capital punishment` and `capital offense/crime`.
   - Keep it distinct from capital city, financial capital and capital letter.
   - No standing Test.

7. **o0698 carry**
   - Add high-value phraseology `carry weight` / `carry little/no/much weight`: have influence, importance, credibility or persuasive force.
   - Keep it separate from literal carrying and from the already-owned `carry out / carry on / carry over`.
   - No standing Test.

## C. Candidates rejected by Self Attack

True or useful material deliberately **not** added merely for completeness:

- `can → could` as a simple Form relation: rejected because `could` has substantial modal-semantic divergence and BF11 already owns the approved dedicated `could` decision model;
- extra `build in / build into / build around / build out` phrasal expansion beyond the high-leverage `build on`;
- `burn bridges`, `burn calories`, and other additional burn phraseology;
- `carbon footprint` as a new default object: useful, but the current carbon/emissions truth plus compositional reading does not justify another final-sweep object;
- generic `cancel → cancellation` family expansion;
- `call it a day/night`, `call the shots`, and other idiom inventory;
- extra `carry a tune / carry the day / carry the can` idiom inventory beyond `carry weight`;
- decorative family trees for simple owners;
- standing Tests for newly admitted irregular Forms.

Reason: truth alone does not earn Expansion attention, and Expansion does not automatically earn Repair debt.

## D. Repair Test Self-Attack

Current canonical standing blueprints: **43** → final proposed set: **18**.

### Keep / retarget — o0601–o0650

1. **o0605 brush** — keep exact `brush up on sth` vs `brush aside sth`.
2. **o0612 buffet** — keep exact noun/verb pronunciation Form identity.
3. **o0614 build** — keep existing exact `build up sth / build sth up` target; do not add a Test merely for `built` or `build on`.
4. **o0616 bulk** — keep `the bulk of sth` vs `in bulk`.
5. **o0621 bump** — keep `bump into sb` vs `bump up sth`.
6. **o0624 burden** — keep `burden sb/sth with sth`.
7. **o0630 bury** — keep exact idiomatic Construction target, centered on `bury oneself in sth`.
8. **o0642 by** — keep `by + deadline` = at or before the deadline.

Remove standing Test debt for:
`browse, brutal, bubble, budget, buffer, bug, bull, bureaucracy, business, butcher, butterfly, buy, bypass, cable`.

### Keep / retarget — o0651–o0700

9. **o0654 calculate** — keep `be calculated to do sth` vs ordinary calculate/estimate.
10. **o0656 call** — retarget broad Core test to exact complement/particle selection: `call for sth / call on sb to do / call sth off`.
11. **o0657 calm** — keep `calm down / calm sb down`.
12. **o0662 campaign** — keep `campaign for/against sth` and the organized-sustained-action boundary.
13. **o0674 canvas** — keep exact `canvas ↔ canvass` confusable Relation.
14. **o0676 capable** — keep `be capable of sth/doing sth`.
15. **o0677 capacity** — keep `in one's capacity as ...` vs ability-capacity.
16. **o0689 care** — keep `care about / care for / would you care for` structure selection.
17. **o0691 careful** — keep `be careful to do sth` vs `be careful with sth`.
18. **o0698 carry** — keep existing `carry out / carry on / carry over` particle-selection target; do not add a separate Test for `carry weight`.

Remove standing Test debt for:
`can, cap, capital, captive, capture, career, carriage, carrier, carrot, cardinal, captain`.

No new standing Test is created for the seven Section B owners solely because the Content is worth preserving.

## E. Shared semantic dependencies for later materialization

Production-only phase records intent but performs no canonical writeback.

Later serialized materialization must refreeze and reconcile at least:

- `by ↔ buy` confusable Relation;
- `campaign ↔ effort` semantic contrast;
- `canvas ↔ canvass` confusable Relation;
- BF11's already-approved `could` modal model as a read dependency, so BF06 does not invent a competing simplistic `can → could` model.

A later dependency change may refine/drop a stale proposal but may not trigger a new broad semantic standard.

## Source pressure-check for genuinely new scope

The final new-scope candidates were additionally pressure-checked against current Cambridge learner/advanced entries:

- `build → built → built` and `build on sth`;
- `burst → burst → burst`;
- `buy → bought → bought`;
- `call sth into question`;
- UK `cancelling/cancelled` vs US `canceling/canceled`;
- `capital` in `capital punishment / capital offense`;
- `carry weight`.

These checks support the lexical facts; they do not replace the frozen KianOS semantic ruler.

## Gate contract

Kian approval of this proposal authorizes only:

- the seven genuinely new owners in Section B;
- the internal corrections in Section A;
- the Test reduction/retarget in Section D;
- freezing o0601–o0700 into Production backlog and marking **Production A complete**.

Because current campaign phase is `PRODUCTION_SWEEP_ONLY`, approval does **not** authorize semantic materialization, FLOB rebuild, B audit or merge now.

After `p`:
- re-sync proposal identity and shared claims;
- record approval;
- freeze BF06;
- set A Production sweep to COMPLETE / no remaining A review range;
- do not enter o0701+ from lane A.
