# Lexical Final-Sweep Production A — o0501–o0600

Status: **POST_FRESH_READ_POST_SELF_ATTACK_BOUNDED_DELTA_GATE**
Semantic standard: `content/lexical/audit/history/2026-09-final-catalog/FINAL_SEMANTIC_FREEZE.md`
Production lane: 词义A
Frozen review base: `main@e18c0853b5b7d1787a23bf8eeaf4797cde93ab72`
Review request commit: `704644b1d920c9e3f6f1a7fe5b502275f91ac38b`
Review workflow: `Lexical Review Bundle Export #35465831000`
Review artifact: `10591485964`
Artifact digest: `sha256:f03bc10cdad5ea65a3b1b75d322cad56a3ddad7b19b947d6d6f84c17917500ed`
Review coverage: **100 / 100 owners, o0501–o0600**
Self-Adversarial Attack: **COMPLETE**
Semantic BLOCKED: **0**

## Result

- 100/100 owners fresh-read under the frozen final ruler.
- 17 in-range owners need bounded correction inside already-approved learner intent.
- 7 owners contain genuinely new material learner scope.
- 77 owners remain NO_CHANGE after Fresh Read + Self Attack.
- Existing canonical Repair Test blueprints: **45**.
- Final standing Test proposal: **22**.
- New standing Repair Tests created merely because Content was admitted: **0**.
- No canonical semantic mutation is authorized in current `PRODUCTION_SWEEP_ONLY` mode.
- No FLOB rebuild / B audit / semantic materialization is performed in this phase.

The arithmetic counts an owner once even when it has both an internal correction and a genuinely new delta (o0520 blow).

## A. Bounded corrections — no new Human scope

These repair layer placement, stable identity, cross-word ownership, reusable structure, or standing-Test inflation already inside accepted learner intent.

1. **o0502 blade**
   - Keep ordinary blade/leaf/knife/propeller branches.
   - Move the specialist beef-cut `blade steak` branch out of default Study into Reference.
   - Do **not** add `shoulder blade` merely because it is a useful phrase; the final sweep does not turn every decodable compound into new default Depth.

2. **o0513 block**
   - Promote the already-visible `block sb/sth from doing sth` pattern from a raw collocation into a stable reusable Construction.
   - No new semantic branch.

3. **o0515 blood**
   - Promote already-visible `bad blood` and `in cold blood` into explicit word-level phraseology so their idiomatic meanings are not hidden under the literal blood Sense.
   - Move the low-leverage historical/temperamental “blood = temperament” and hunting-rite verb material out of default Study into Reference.

4. **o0517 bloom**
   - Keep flower / flowering and ecological bloom as active learner branches.
   - Move the rare transitive “make flourish / make bloom” branch out of default Study into Reference.

5. **o0520 blow**
   - Move the low-current-value botanical “flower/bloom” verb branch to Reference.
   - The irregular Form addition is separately listed in Section B.

6. **o0525 blur**
   - Promote the already-visible `blur the line/distinction between A and B` pattern into a stable Construction/phraseology object.
   - No new meaning.

7. **o0533 bolster**
   - Retire the one-member pseudo-Relation whose payload only restates that abstract `bolster` means support/strengthen.
   - Keep that truth Word-owned; do not invent a thesaurus-style cross-word Relation.

8. **o0545 bore**
   - Preserve lexical `bore` (make someone bored / drill / technical noun) as its own Word owner.
   - Coalesce the surface form `bore` = past tense of `bear` with the already-approved BF04 `bear → bore → borne/born` Form identity.
   - This is shared Form ownership repair, not a new BF05 delta.

9. **o0546 born**
   - Stop presenting `born` as a generic active Sense “past participle of bear”.
   - Represent it as the birth-conditioned Form in the already-approved BF04 bear identity; ordinary non-birth past participle remains `borne`.
   - Preserve the useful born/borne learner boundary.

10. **o0547 borrow**
    - Move the current `borrow vs lend` pseudo-Construction into a genuine cross-word Relation.
    - Keep Word-owned `borrow sth from sb/sth` structure.
    - Shared boundary: borrow = receive/take temporarily; lend = give temporarily.

11. **o0553 bottom**
    - Promote already-visible `the bottom line`, `get to the bottom of sth`, and `bottom out` out of the raw collocation pile into explicit phraseology/Construction ownership.
    - No new semantic scope; the strings are already Current learner intent.

12. **o0555 bound**
    - Add the exact `be bound by sth` structure under the already-owned “constrained/obliged” branch.
    - Coalesce the surface identity `bound` = past/participle of `bind` with BF04’s already-approved `bind → bound → bound` Form truth.
    - Keep lexical `bound` (likely/certain; destination; limit; leap) separate from the inflected-form identity.

13. **o0571 brass**
    - Move already-visible `top brass` away from the literal metal Sense into explicit phraseology with its real meaning: senior leaders / people in highest authority.
    - Preserve literal brass and brass-instrument senses.

14. **o0579 breath / o0580 breathe**
    - Preserve the accepted noun/verb confusable boundary.
    - Make the existing `breath ↔ breathe` Relation reciprocal from the `breathe` side instead of source-only.
    - Keep Word-owned breath phrases and `breathe life into` separate.

15. **o0590 bright ↔ smart**
    - Preserve the existing useful contrast: `bright` emphasizes quick understanding/learning; `smart` is broader/more conversational and may emphasize cleverness.
    - Make the accepted Relation reciprocal from the smart side rather than source-only.

16. **o0599 broom**
    - Keep ordinary `broom = 扫帚` as the active learner branch.
    - Move the specialist shrub/plant Sense out of default Study into Reference.

## B. Genuinely new final-standard delta — requires bounded Kian approval

These seven owners survived both UNDER-UPGRADE and OVER-UPGRADE attacks.

1. **o0509 bleed**
   - Add irregular Form `bleed → bled → bled`.
   - Add the common figurative/business branch `bleed money/resources/jobs`: lose or drain them continuously/seriously.
   - Expansion / familiar-new; no standing Repair Test.

2. **o0520 blow**
   - Add irregular Form `blow → blew → blown`.
   - Expansion/Form only; no new standing Test.

3. **o0573 breach**
   - Add the common transitive legal/security branch: `breach a law / rule / agreement / contract / security` = violate or break it.
   - Add the reusable formal phrase `be in breach of sth`.
   - Keep physical “make an opening/break through” as a separate existing branch.
   - No standing Repair Test.

4. **o0575 break**
   - Add irregular Form `break → broke → broken`.
   - Expansion/Form only; no new standing Test.

5. **o0581 breed**
   - Add irregular Form `breed → bred → bred`.
   - Expansion/Form only; no new standing Test.

6. **o0592 bring**
   - Add irregular Form `bring → brought → brought`.
   - Expansion/Form only; no new standing Test.

7. **o0595 broadcast**
   - Add Form identity: past/past participle `broadcast` or `broadcasted`, with `broadcast` the common unchanged form.
   - Make the already-implied Core “传播” explicit as a current familiar-new public-dissemination branch: to tell/spread information widely, not only transmit radio/TV.
   - No standing Repair Test.

## C. Candidates rejected by Self Attack

True/useful material deliberately **not** added merely for completeness:

- `blade → shoulder blade` as a new default phrase object;
- `bleed sb/sth dry` and extra figurative bleed phraseology beyond the common drain/loss branch;
- `blow away / blow over / blow out` broad phrasal expansion;
- `botany → botanical`;
- `brass` specialist memorial/adjective expansion;
- `brilliant = excellent!` as a new regional conversational branch;
- `broad → broaden`;
- extra `break` idioms beyond the already-owned high-value constructions;
- `brother → brethren`;
- generic family trees for simple owners;
- a new standing Test for every admitted irregular Form.

Reason: truth alone does not earn Expansion attention, and Expansion does not automatically earn Repair debt.

## D. Repair Test Self-Attack

Current canonical standing blueprints: **45** → final proposed set: **22**.

### Keep / retarget — o0501–o0550

1. **o0503 blame** — keep exact `blame sb for sth / blame sth on sb`.
2. **o0506 blast** — keep exact idiom `have a blast`.
3. **o0507 blaze** — keep `blaze a trail`.
4. **o0510 blend** — keep `blend in` vs `blend with/into`.
5. **o0512 blind** — keep `turn a blind eye to`.
6. **o0523 blunder** — keep `blunder into sth`.
7. **o0527 board** — keep `on board` vs `on the board`.
8. **o0528 boast** — keep `boast of/about sth` vs transitive `boast sth`.
9. **o0531 boil** — keep `boil down to sth`.
10. **o0544 border** — keep literal/figurative `border on`.
11. **o0546 born** — retarget to the shared BF04 `bear → bore → borne/born` Form decision.
12. **o0547 borrow** — retarget to the shared `borrow ↔ lend` direction/argument Relation.

Remove standing Test debt for:
`blank, bloody, bloom, blue, body, bomb, bond, bone, boot, bore, both`.

### Keep / retarget — o0551–o0600

13. **o0551 bother** — keep `bother to do / bother doing` vs transitive `bother sb`.
14. **o0552 bottle** — keep `bottle up feelings/emotions`.
15. **o0555 bound** — keep `bound to do / bound for / bound by` exact structure decision after correction.
16. **o0557 bow** — keep the sense-conditioned /baʊ/ vs /bəʊ~boʊ/ Form target.
17. **o0564 brace** — keep `brace oneself for sth`.
18. **o0569 brand** — keep `brand A as B`.
19. **o0579 breath** — retarget to exact `breath ↔ breathe` noun/verb Relation discrimination.
20. **o0580 breathe** — keep `breathe (new) life into sth`.
21. **o0582 breeze** — keep `breeze through sth`.
22. **o0588 brief** — keep `brief sb on sth`.

Remove standing Test debt for:
`bowling, bracket, brake, breach, break, breakdown, breed, bright, bring` (both old bring blueprints), `broadcast, bronze`.

No new standing Test is created for the new bleed/blow/breach/break/breed/bring/broadcast Content solely because it is worth preserving.

## E. Shared semantic dependencies for later materialization

Production-only phase records intent but performs no writeback.

Later serialized materialization must refreeze and reconcile at least:

- BF04 `bear → bore → borne/born` Form identity;
- BF04 `bind → bound → bound` Form identity;
- `borrow ↔ lend` direction/argument Relation;
- `breath ↔ breathe` confusable Relation;
- `bright ↔ smart` semantic contrast.

A later dependency change may refine/drop a stale proposal but may not trigger a new broad semantic standard.

## Gate contract

Kian approval of this proposal authorizes only:

- the seven genuinely new owners in Section B;
- the internal corrections in Section A;
- the Test reduction/retarget in Section D;
- freezing o0501–o0600 into Production backlog.

Because current campaign phase is `PRODUCTION_SWEEP_ONLY`, approval does **not** authorize semantic materialization, FLOB rebuild, B audit or merge now.

After `p`:
- re-sync proposal identity and shared claims;
- record approval;
- freeze this batch;
- immediately move A to o0601–o0700.
