# Lexical Independent Semantic Audit — o2425–o2624

- **Status:** HOLD_FOR_SOL
- **Scope:** `o2425–o2624` — 200 Word owners
- **Production handoff:** `content/lexical/semantic-review/o2375-o2624.md` (audited only `o2425–o2624`)
- **Owner-read / Router source HEAD:** `5003ffb6246183b02e30808ca49e987bbd302694`
- **Pre-write HEAD:** `81abe3ca297649c99f27ed32992f17e366dba95c`
- **Content contract blob SHA:** `9ed963e15f9a8a5412012b915881298d2e95dd68`
- **Audit contract blob SHA:** `a37754c0e3061a1dc8902145b6dca07e902f990a`
- **Router spec blob SHA / router version:** `94fa18d48bf4f42f2d8c51bf15705244efb14c48` / `1.0.0`
- **Router implementation blob SHA:** `68b9ceee82fdebc342a7d56f487ffec4c8552a8f`
- **Production handoff blob SHA:** `60b18f79339c512153af2294ed8c5906cd8e3d14`
- **Production semantic baseline:** `5bf7f9d0995573a3fb5dc7fab2113cd7adf4f29c`
- **Risk-manifest SHA256:** `fd153b466f421622f78fe78148e27776423e3a4add47cbf11333500ebf7eac61`
- **Blind-view SHA256:** `eaf331a1a9f591f8ec07847adb266c9f5ccdb7042797110f186bea7e7aa520bc`
- **Dependency-inventory SHA256:** `0cdff4d69d8e8660f97701c23822395f849df48577a85c34fbdeff81f5053348`
- **Working artifact:** Actions run `34827588596`, artifact `10341275247`, digest `sha256:49d46cb4d2b5e48dd3457ab1fcccf34570207382b9605518f6da4fcf6390f5e3`
- **Current-first provisional receipt:** audit-branch commit `78cfed14e749269eaa775f9edadb59d61ef22f02` (`content/lexical/semantic-audit/work/o2425-o2624.pass-a.md`; must not land on `main`)
- **Calibration:** PASS
- **Blind-first:** **NOT_ENFORCED** — detailed Production rationale for this scope had already been exposed in the immediately preceding batch when the whole `o2375-o2624.md` handoff was fetched during that batch's Pass B
- **Audit mode:** STRICT

## Evidence-strength caveat

This batch must **not** be cited as strong blind-review evidence. The isolated workflow itself maintained the same physical separation used by the strongest prior batches: its uploaded artifact contained no Production rationale, only the Router risk manifest, blind view, all 200 Current Natural Word owners, and their Current dependency owners. However, before this batch began, the immediately preceding audit had fetched the entire `o2375-o2624.md` Production handoff into conversation context while auditing `o2375–o2424`; that response necessarily exposed the detailed rationale for `o2425–o2624` as well.

The reviewer therefore still performed a Current-owner-first pass and froze it on the audit branch before the current batch's explicit Production comparison, but the prior-context exposure means the formal state is `BLIND_FIRST_NOT_ENFORCED`. This is a provenance limitation, not a reason to weaken or discard the semantic findings.

## Method and coverage

The frozen STRICT ruler is unchanged. Machine routing classified this scope as:

- Production `UPGRADE`: **23**
- complex Production `NO_CHANGE`: **157**
- simple Production `NO_CHANGE` candidates: **20**
- unique machine-mandatory owners: **180**
- Form/identity mandatory: **51**
- Relation/anchor/Core↔sense mandatory: **37**

Coverage completed:

```text
scope owners read / fast-gated: 200/200
Production UPGRADE reverse-reviewed: 23/23
complex NO_CHANGE full-reviewed: 157/157
simple NO_CHANGE fast-gated: 20/20
deterministic simple deep sample: 10/20
simple sample false-pass: 0/10
unique machine-mandatory owners reviewed: 180/180
effective full-depth owners: 190/200
mandatory Form / ownership / identity cases: 51/51
mandatory Relation / anchor / Core↔sense cases: 37/37
independent Relation owners read: 14/14
sense-registry dependency shards read: 7/7
facts dependency shards read: 7/7
collocation dependency shards read: 7/7
spelling-lookup dependency shards read: 7/7
word-id lookup shards materialized: 138
word shards materialized: 25
```

Deterministic simple deep sample:

`o2432 ideology`, `o2434 idiot`, `o2444 illness`, `o2452 imaginative`, `o2493 income`, `o2529 influential`, `o2534 infrastructure`, `o2555 innumerable`, `o2570 instantaneous`, `o2619 intricate`.

All ten passed. STRICT therefore did not expand the remaining ten simple owners to full depth.

The Current-first provisional result was frozen on the isolated audit branch at `78cfed14…`. The receipt explicitly records the prior-context blindness compromise; it does not claim `PRODUCTION_RATIONALE_NOT_OPENED`.

## Final summary

- Production `UPGRADE` reverse-review: **23/23**
  - `FLIP_TO_NO_CHANGE`: **0/23**
  - `REFINE_UPGRADE`: **2/23** — `o2479 impression`, `o2582 insure`
  - `IDENTITY_RISK`: **2/23** — `o2552 inner`, `o2567 instalment`
  - directionally accepted without further semantic refinement: **19/23**
- complex Production `NO_CHANGE` false-pass: **22/157 = 14.0%**
- deterministic simple sample false-pass: **0/10 = 0%**
- final simple false-pass among fully deep-reviewed sample: **0/10**
- `BLOCKED`: **0**
- canonical Word / Sense / Relation / Form mutations by Audit Lane: **0**

Final disposition: **HOLD_FOR_SOL** because two Production-upgrade targets contain genuine stable Word/Sense identity ambiguity.

## Production UPGRADE reverse-review

### `REFINE_UPGRADE` — 2

#### `o2479 impression`

Production is correct to restore ordinary `impression` = idea/opinion/feeling and the effect left on someone. Current learner truth is dominated by printing/dental L3 material while the ordinary idea branch survives only as deprecated stable truth.

Additional required closure: the already-published high-value construction `be under the impression (that...)` currently has `source_sense_id:null`. Implementation must re-anchor it to the restored ordinary idea/belief branch rather than merely restoring senses around a hollow construction.

#### `o2582 insure`

Production is correct to teach `insure` primarily as provide/protect with insurance and distinguish it from default `ensure` = make certain. Current still exposes `insure = make certain` as an equal active L2 sense and has `insure + that clause` attached to it.

Additional required closure: this new `insure ↔ ensure` learner boundary must be reciprocal from both Word entry points. Do not create another source-only accepted contrast. This is Relation closure, not a request to merge the two lexemes.

### `IDENTITY_RISK` — 2

#### `o2552 inner`

Production correctly detects that Current over-collapses `inner` into one physical-location adjective. Stable history, however, contains four merged branches pointing into active `sense:inner:271dc7a9c5625403`, including:

- inward/closer-to-center adjective;
- **noun** `the inner part`;
- essential/innermost adjective;
- central/exclusive-to-a-center adjective.

Production asks to restore high-transfer figurative/central uses such as inner thoughts/feelings and inner/core/central while reusing the surviving stable branch unless a genuine conflict appears. Audit finds that conflict: there is cross-POS history plus learner-distinct adjective branches. Sol must decide whether the final learner model deliberately broadens one adjective ID, restores selected prior identities, or performs a bounded split/unmerge. Executor must not choose that lifecycle policy mechanically.

Relation closure is also required: the accepted `inner ↔ interior` and `inner ↔ internal` Relation owners each currently contain only the `inner` source-side Word view.

#### `o2567 instalment`

Production correctly wants ordinary `instalment` = one of a series of payments or serial parts/episodes and a BrE/AmE boundary `instalment / installment`.

The Form boundary is not a plain spelling-string fact in Current Truth. `installment` already exists as an independent Main Word at **`o5902`**, with `word:installment` and its own two stable deprecated sense identities for the payment and serial-part meanings. `instalment` likewise has its own stable history.

Therefore implementation must preserve the current Word-identity boundary until Sol explicitly reconciles it. Audit does **not** authorize aliasing, merging, retiring, or silently redirecting either Main Word.

### Direction accepted — 19

The remaining Production upgrades are semantically justified as written, subject to ordinary stable-ID reuse and final learner-object readback:

`o2445 illuminate`, `o2449 image`, `o2461 immune`, `o2462 impact`, `o2473 import`, `o2478 impress`, `o2483 in`, `o2495 increase`, `o2500 independence`, `o2509 indispensable`, `o2512 induce`, `o2515 industrialise`, `o2525 infinite`, `o2532 information`, `o2563 inspiration`, `o2568 instance`, `o2576 instruction`, `o2581 insurance`, `o2611 interrupt`.

Notable lifecycle notes:

- `increase`: the merged transitive branch is semantically continuous with the surviving broad increase verb; the Production target is Form pronunciation, not a demand for a new semantic ID.
- `industrialise`: merged intransitive history remains within the same industrialization event family; no second Main Word `industrialize` was found in Current spelling lookup.
- `interrupt`: historical ordinary process/temporary-stop definitions are merged into the active speech-interruption verb. Production's bounded broadening back to the general interruption event can reuse the surviving stable branch; Audit does not require a split solely because the historical wording was more general.
- `induce` and `instance`: their current construction anchors are defective/deprecated or null, but Production explicitly names the high-value construction/alignment target, so no additional semantic refinement is required beyond implementing that target correctly.

## Production `NO_CHANGE` false-passes — 22

### 1. `o2430 identify` — Relation reciprocal closure

Accepted `identify ↔ distinguish` contrast exists only as an `identify` Word view. `identify` establishes what something is; `distinguish` tells entities/features apart. Final Relation truth must be visible from both learner entry points.

### 2. `o2437 ignite` — familiar-new transitivity branch

Current exposes transitive `cause something to start burning` plus figurative arousal, but misses ordinary intransitive `ignite` = start burning. Learner-dictionary spot verification confirms `ignite` is ordinarily both intransitive and transitive for starting to burn/explode.

### 3. `o2454 imitate` — Relation reciprocal closure

Accepted `imitate ↔ copy` contrast is source-only from `imitate`. The target distinction is learner-worthy: imitate commonly targets behavior/style/sound, while copy also covers reproduced texts/objects.

### 4. `o2471 implicit` — Relation reciprocal closure

Accepted `implicit ↔ explicit` sense-bound antonym is source-only. The same relation must be reachable from `explicit` without duplicating semantic truth.

### 5. `o2472 imply` — missing reciprocal directionality relation

Current `imply` exposes its own `imply ↔ entail` relation but not the accepted `infer ↔ imply` directionality contrast. The crucial learner boundary is source/speaker suggestion versus reader/observer conclusion.

### 6. `o2476 impose` — Relation reciprocal closure

Accepted `impose ↔ inflict` contrast is source-only. `inflict` normally selects harm/pain/suffering; `impose` has broader burden/rule/condition space.

### 7. `o2489 incidentally` — Core / active-truth mismatch

Core promises `by the way or by chance`, while active learner truth contains only the discourse/additional-point use. Current major learner-dictionary verification strongly supports the discourse use; if `by chance` is deliberately retained it needs an explicit lower branch/evidence, otherwise it should leave Core.

### 8. `o2492 inclusive` — familiar-new modern sense

Current only exposes the containing/everything/stated-limits sense. It misses a now-standard learner meaning: a group, organization, language, or society that includes different types of people and treats them fairly/equally. External learner-dictionary verification treats this as ordinary C1/current usage.

### 9. `o2513 indulge` — familiar-new transitive person branch

Current is desire/impulse-led and preserves only a merged `enjoy to excess` history. It misses ordinary transitive `indulge sb` = let/pamper someone have or do what they want. External learner-dictionary verification supports both self-indulgence and indulging another person.

### 10. `o2516 industry` — Core / lifecycle mismatch

Core explicitly promises `industry ... also hard work`, but the stable diligence/working-hard noun branch is deprecated and absent from active learner senses. Either restore it at an appropriate lower priority or remove the unsupported Core promise.

### 11. `o2522 infer` — hollow constructions + one-sided Relation family

Both `infer sth from sth` and `infer that + clause` are published with `source_sense_id:null` despite a matching active infer-from-evidence sense. The accepted `infer ↔ imply` and `infer ↔ deduce` relations are also source-only.

### 12. `o2524 inferior` — Relation reciprocal closure

Accepted sense-bound `inferior ↔ superior` antonym is source-only. Keep noun/anatomical/technical material outside this relation unless separately matched, as the existing relation rule already specifies.

### 13. `o2527 inflict` — hollow construction + missing reciprocal Relation

`inflict sth on/upon sb` exists with `source_sense_id:null` even though the active verb means impose something unpleasant. `inflict` is also the target of accepted `impose ↔ inflict`, but the relation is invisible from this entry point.

### 14. `o2535 ingenious` — confusable reciprocal closure

Accepted `ingenious ↔ ingenuous` boundary is one-sided. The high-value distinction — inventive/clever versus innocent/candid/naively straightforward — must be available from both words.

### 15. `o2554 innovation` — Relation reciprocal closure

Accepted `innovation ↔ invention` contrast is source-only: innovation emphasizes introduction/adoption of something new, invention the invented creation/result.

### 16. `o2564 inspire` — familiar-new creative-source branch

Current active senses cover encouragement/confidence, causing feelings, and rare technical inhalation. The stable branch `give someone the idea for a book/work/product` is deprecated even though Core explicitly emphasizes creative inspiration. External learner-dictionary verification confirms this is ordinary B2/high-transfer usage, often passive (`be inspired by ...`).

### 17. `o2575 instruct` — construction attached to wrong sense

`instruct sb in sth` is currently attached to `make aware of`. External learner-dictionary verification defines the construction as teach/train someone in a subject, skill, or procedure. Re-anchor to a teaching/instruction branch rather than altering the construction wording to fit the wrong sense.

### 18. `o2578 instrumental` — hollow high-value construction

`be instrumental in sth/doing sth` is published with `source_sense_id:null` despite a matching active adjective `serving as a means or contributing to a result`. Attach it to that branch.

### 19. `o2597 intercourse` — register / learner-priority inversion

Current Core leads with `communication or interaction between people`, while active senses contain social intercourse and sexual activity. Major learner dictionaries mark the social/conversation sense **old-fashioned formal**, while sexual intercourse is the modern default learner interpretation. Preserve the historical sense, but do not let it lead unmarked Core.

### 20. `o2598 interest` — hollow construction

`in the interest(s) of sth` is published with `source_sense_id:null` despite an active noun branch for advantage/benefit/welfare/stake. Attach the construction to that sense.

### 21. `o2604 interior` — missing reciprocal `inner ↔ interior` view

Current `interior` has no Relation ref for the accepted `inner ↔ interior` contrast. The relation owner contains only the `inner` source-side learner view.

### 22. `o2607 internal` — incomplete reciprocal Relation family

Current `internal` exposes its own `internal ↔ eternal` confusable relation, but the accepted `inner ↔ internal` contrast is invisible from the `internal` entry point. Add reciprocal visibility without duplicating relation semantics.

## Relation-family readback

All **14/14** independent Relation owners materialized for this scope were read.

- Same-word Form control: `intimate` adjective/noun final `/-mət/` vs verb `/-meɪt/` — valid same-lexeme heteronym truth; no reciprocal cross-word requirement.
- Cross-word accepted relations reviewed included:
  - `identify ↔ distinguish`
  - `impose ↔ inflict`
  - `inner ↔ interior`
  - `inner ↔ internal`
  - `inferior ↔ superior`
  - `internal ↔ eternal`
  - `imitate ↔ copy`
  - `infer ↔ deduce`
  - `infer ↔ imply`
  - `imply ↔ entail`
  - `ingenious ↔ ingenuous`
  - `innovation ↔ invention`
  - `implicit ↔ explicit`

Every cross-word Relation owner in this materialized set contains only a source-side `word_view`. Audit therefore treats reciprocal learner-entry visibility as a systematic closure requirement, not as permission to duplicate or recreate relation meaning.

## External lexical verification

External dictionaries were used only to confirm ordinary semantic/Form truth; they are not stable-ID or repository authority. Cambridge learner-dictionary spot checks supported the following audit calls:

- `ignite`: ordinary `[I or T]` start/cause to start burning;
- `inclusive`: current people/group sense that includes different types of people fairly/equally;
- `indulge`: both self-indulgence and transitive indulging another person;
- `instruct sb in sth`: teach someone how to do/use something;
- `inspire`: ordinary provide-an-idea/creative-source branch;
- `intercourse`: sexual meaning current/default; social conversation sense marked old-fashioned formal;
- `incidentally`: dominant learner-facing discourse/additional-point use.

These checks validate the semantic judgments above but do not override Current stable identity.

## Drift gate

Between owner-read HEAD `5003ffb6246183b02e30808ca49e987bbd302694` and pre-write HEAD `81abe3ca297649c99f27ed32992f17e366dba95c`, `main` advanced by six commits. The net Lexical changes were the mechanical implementation/readback for earlier range `o1125–o1374`, execution-state updates, and no changes to:

- any target owner `o2425–o2624`;
- `content/lexical/semantic-review/o2375-o2624.md`;
- the Content Contract, Audit Contract, Router Spec, or Router implementation;
- this batch's Relation owners or cross-word target dependencies.

Therefore the audit judgment remains valid against the frozen owner-read snapshot and no target re-review was required before write.

## Sol handoff

Before any executor implements this audit delta, Sol must resolve:

1. `o2552 inner` — whether the over-merged stable history should remain one deliberately broad adjective identity or undergo a bounded split/unmerge; do not mechanically reactivate every historical branch.
2. `o2567 instalment` / `o5902 installment` — how the two existing Main Word identities should represent the BrE/AmE spelling relationship; do not alias, merge, or retire either word without an explicit lifecycle decision.

After those two identity decisions, the remaining changes are bounded semantic/Form/Relation repairs. Final acceptance still requires integrated learner-object readback; validator green alone is insufficient.
