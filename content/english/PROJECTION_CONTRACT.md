# English Projection Contract — RETIRED COMPATIBILITY NOTE

Status: **RETIRED AS AUTHORITY**

This path remains only so historical references fail safely.

English no longer has a separate Projection architecture layer or an independent Projection contract.

Current authority is:

```text
English cognition / learning semantics
→ content/english/LEARNING_CONTRACT.md

canonical task / learning Content
→ content/english/ canonical owners and module owners

learner-facing Visual / presentation
→ static-web/PRESENTATION_CONTRACT.md
→ static-web/ENGLISH_PRODUCT_BRIEF.md for accepted English surface blueprints

shared Engineering / Runtime behavior
→ SYSTEM_CONTRACT.md
→ exact implementation owner
```

## Current projection rule

Projection is optional derived presentation, not a semantic owner.

```text
simple case:   canonical Content → renderer
stateful case: canonical Content → derived presentation → renderer
```

Derived presentation may hide, foreground or compress already-approved Content when learner state requires it. It must not invent Knowledge, Learning Logic or a second content copy.

## English / Lexical relationship

Learner-facing product structure is:

```text
English
└─ Vocabulary / Lexical
```

Lexical keeps an independent backend canonical owner at `content/lexical/` because its asset scale and semantics justify separate maintenance. Backend ownership independence does **not** make Lexical a fourth learner-facing subject.

English task lexical failures may hand off into Lexical and return to the exact originating task.

## Shared-task reuse

Different data sources do not justify duplicate presentation/runtime when task semantics are the same.

Example:

```text
Reading A ───────┐
External Reading ├→ same accepted Reading blueprint / task family
```

Do not extend this retired file with new product or learning rules. Change the current canonical owner instead.
