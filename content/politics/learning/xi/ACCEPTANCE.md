# Politics Xi Acceptance

Status: CURRENT  
Scope: 习近平新时代中国特色社会主义思想 C00–C17  
Standard: root `LEARNING_ACCEPTANCE.md`  
Role: Xi scoped Acceptance Truth

This file owns current acceptance claims for the independently continued Xi subject. It does not own Source Truth, Politics-wide contracts, Work Cursor, runtime code, or Kian's private learner progress.

---

## Gate status

```text
S  PASS
K  PASS
L  PASS
P  PASS
R  PASS
E  UNTESTED   ← earliest unresolved
U  UNTESTED   ← learner-only
```

Supplemental content conclusion: `C00–C17 Current content closure PASS`.

Allowed conclusion:

> **Xi now has accepted Current Source/Knowledge, complete first-round content, whole-book hierarchy-first Learning Logic, learner-facing Projection, and Xi-specific executed Runtime. Evidence semantics and real learner validation remain unaccepted.**

Do not promote this to module-ready or learner-validated before E is independently accepted and U comes from real Kian use.

---

## Accepted upstream｜S/K/L/P

Xi has 60 canonical Natural Units and preserves the frozen first-round source boundary:

```text
Chengfeng  continuous first-round mainline + semantic authority
Suyi       compact framework / exactness / past-exam cross-check only
Xiao1000   verification / transfer evidence, not first-learning order
KianOS     hierarchy / role / boundary / selective precision companion
```

Current priority inventory remains:

```text
ordinary P0  6
ordinary P1  36
ordinary P2  12
C02 frozen-pilot units without ordinary priority  6
```

P0/P1 ownership is not render entitlement or permanent Memory admission. P2 may remain source-primary/deferred.

### L PASS

`learning-review.json` records **C00–C17 = 18 KEEP / 0 REPAIR / 0 BLOCKED**. The accepted Xi first-round rule is:

> **先分层级和身份，再做固定表述精确化。**

Strongest attacks—C02 four-list collision, C07 duplication, C15 Taiwan role-chain overload and C17 overlap with C03—were already controlled by role/layer/boundary semantics without adding learner operations.

### P PASS after one repair

`projection-review.json` records **C00 REPAIR / C01–C17 KEEP**.

Real P defect:

```text
C00 hierarchy = structured array of {role, meaning}
→ generic component String(value)
→ non-render-safe [object Object] risk
```

`politicsXiProjection.mjs` now normalizes structured Xi hierarchy rows at Projection only. Accepted chapter Content/Learning remains unchanged. The Xi Projection audit verifies render-safe hierarchy, no `content_support` leak, explicit external Chengfeng handoff and progressive guide/closure disclosure.

---

## R｜Fresh Runtime re-acceptance — PASS after one repair

Mode: `EXECUTED + ADVERSARIAL`  
Subject-specific journey: `static-web/scripts/test-politics-xi-runtime.mjs`

### First real R failure

The first Xi-specific browser run passed **72 smoke checks** across C00–C17:

- subject/chapter identity;
- Natural Unit presence;
- external-primary Chengfeng handoff;
- intended generic Xi runtime rather than Marxism cognitive workspace.

It then failed at:

```text
xi_config_with_1_questions_exists
```

Root cause:

```text
buildPoliticsUnitReturnConfigs
→ conservative NATURAL_UNIT_SAFE_FALLBACK already existed
→ allowlist included Marxism / History / Mao only
→ Xi rendered valid Current Xiao1000 questions
→ Xi received no executable Unit Return config
```

This was classified as a **Runtime defect**, not a reason to reopen S/K/L/P.

### Narrow repair

`static-web/src/lib/politicsUnitReturn.mjs` now allows Xi to use the existing safe fallback:

```text
NATURAL_UNIT_SAFE_FALLBACK
mastery_claim = NONE
```

The repair does not invent finer question→node mappings, change Xi teaching/projection assets, or enable Ethics-Law. Natural-Unit precision remains explicit where finer mapping is not source-validated.

### Executed Xi R evidence

Repair head: `aefaf829743d0b38b8084ce1e82f1b74b35c1ffd`

- Static Web Politics QA #572 (`34773873052`) → **PASS**
- Politics Functional First Journey #83 (`34773873064`) → **PASS**
- Politics Xi Runtime Journey #2 (`34773873057`) → **PASS**
- Xi Runtime artifact → **90/90 checks PASS**

The Xi-specific journey proves:

1. **C00–C17 runtime identity** — all 18 chapters preserve Xi identity, Natural Units, external Chengfeng handoff and generic subject runtime.
2. **Clean verification path** — one real Xi Unit executes five clean Xiao1000 first attempts; all store `STABLE`, Unit Return appears, no Wrong/Uncertain handoff debt is created, and the UI keeps `不等于长期掌握` explicit.
3. **Meaningful resume** — the actual next pending Xi question is stored as `VERIFY` and restored active after refresh with subject/chapter/unit identity intact.
4. **Wrong/Uncertain route** — a real problem first attempt remains distinct, routes to the owning source and stores `REPAIR_SOURCE`.
5. **Repair return** — the learner returns to the interrupted question/result with repair state visible.
6. **First-attempt immutability** — the stored first-attempt object remains unchanged through repair-return.

Shared Politics Functional First is regression evidence only. Xi R PASS is owned by the Xi-specific executed journey.

---

## R boundary / negative space

R PASS does **not** mean:

- every Xi question has been exhaustively exercised;
- STABLE means mastery;
- repair turns a historical Wrong/Uncertain into first-attempt success;
- Return Packet attribution, persistence failure and downstream Evidence interpretation are independently accepted;
- later fresh/holdout transfer has been executed;
- Kian has studied or mastered Xi.

---

## Stage boundary

```text
S/K/L/P/R  PASS
E          UNTESTED / earliest unresolved
U          UNTESTED / learner-only
```

Next legitimate Xi work is **E｜Evidence**. R execution is strong prior evidence, but E must independently attack first-attempt durability, repair≠mastery, routing/provenance, persistence, private learner state and Return Packet meaning.

Do not reopen accepted S/K/L/P/R without new source truth, question evidence, real learner friction, or a concrete responsible defect.

---

## Truth boundaries

### Artifact Truth
- teaching/review assets → `content/politics/learning/xi/`
- Xi Projection adapter → `static-web/src/lib/politicsXiProjection.mjs`
- Unit Return runtime → `static-web/src/lib/politicsUnitReturn.mjs`
- Xi Runtime journey → `static-web/scripts/test-politics-xi-runtime.mjs`
- shared contracts → `content/politics/LEARNING_CONTRACT.md` + `content/politics/INTERACTION_CONTRACT.md`

### Acceptance Truth
This file.

### Learner Truth
Private browser / Return Packet / conversation evidence only.

> **Accepted Runtime is not Xi Evidence acceptance, mastery, or U evidence.**
