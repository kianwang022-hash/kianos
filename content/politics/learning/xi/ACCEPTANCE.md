# Politics Xi Acceptance

Status: CURRENT  
Scope: 习近平新时代中国特色社会主义思想 C00–C17  
Standard: root `LEARNING_ACCEPTANCE.md`  
Role: Xi scoped Acceptance Truth

This file owns current acceptance claims for the independently continued Xi subject. It does not own Source Truth, Politics-wide contracts, Work Cursor, runtime code, or Kian's private learner progress.

---

## Gate status

```text
S  PASS
K  PASS
L  PASS
P  PASS
R  PASS
E  PASS
U  UNTESTED   ← learner-only
```

Supplemental content conclusion: `C00–C17 Current content closure PASS`.

Allowed conclusion:

> **Xi is module-ready for learner test under the Current Politics surface/evidence contracts: S/K/L/P/R/E are independently accepted. U remains UNTESTED until Kian actually uses a named Xi learner path.**

Do not promote this to learner-validated.

---

## Accepted upstream｜S/K/L/P/R

Xi preserves the first-round source boundary:

```text
Chengfeng  continuous first-round mainline + semantic authority
Suyi       compact framework / exactness / past-exam cross-check only
Xiao1000   verification / transfer evidence, not first-learning order
KianOS     hierarchy / role / boundary / selective precision companion
```

Xi has 60 canonical Natural Units: 6 ordinary P0, 36 ordinary P1, 12 ordinary P2, plus 6 frozen-pilot C02 units without the ordinary priority field. Priority ownership is not render entitlement or permanent Memory admission.

### L｜PASS

`learning-review.json` records **18 KEEP / 0 REPAIR / 0 BLOCKED**. The governing first-round rule is:

> **先分层级和身份，再做固定表述精确化。**

The accepted path handles Xi's highest-risk hat/list collisions through semantic role, hierarchy and boundary before exact-wording precision.

### P｜PASS after one repair

`projection-review.json` records **C00 REPAIR / C01–C17 KEEP**.

C00's structured `{role, meaning}` hierarchy was not render-safe under the generic component's `String(value)` behavior. `politicsXiProjection.mjs` now normalizes that structure at Projection only. Accepted Content/Learning assets remain unchanged. The Xi Projection audit guards render safety, no `content_support` leakage, Chengfeng external-primary ownership and progressive disclosure.

### R｜PASS after one repair

The first Xi-specific Runtime journey passed 72 C00–C17 smoke checks, then failed because `NATURAL_UNIT_SAFE_FALLBACK` excluded Xi. `politicsUnitReturn.mjs` now includes Xi while Ethics-Law remains excluded and `mastery_claim` remains `NONE`.

Final R evidence:

- Static Web Politics QA #572 (`34773873052`) → PASS;
- Politics Functional First Journey #83 (`34773873064`) → PASS;
- Politics Xi Runtime Journey #2 (`34773873057`) → PASS;
- Xi Runtime artifact → **90/90 checks PASS**.

R proves clean verification, meaningful resume, Wrong/Uncertain source repair-return and first-attempt immutability. It does not manufacture Evidence acceptance or U.

---

## E｜Fresh Evidence re-acceptance — PASS after one repair

Review owner: `content/politics/learning/xi/evidence-review.json`  
Model audit: `static-web/scripts/audit-politics-xi-evidence.mjs`  
Browser journey: `static-web/scripts/test-politics-xi-evidence.mjs`

### Whole-module Evidence model

```text
C00–C17                 18 / 18 chapters
Unit Return configs     52
question evidence slots 215
model checks            540
failures                0
```

The model audit independently verifies:

- first meaningful attempt is immutable once recorded;
- WRONG and UNCERTAIN remain distinct;
- STABLE remains this-pass stability only;
- `mastery_claim = NONE`;
- evidence precision stays bounded to validated Natural Unit / canonical-node scope;
- out-of-scope evidence is rejected.

### First real browser E defect

Xi Wrong/Uncertain events preserved subject/chapter/unit/question/outcome but did not carry stable repair-source provenance. The underlying `PoliticsEvidenceEnhancer` already supported:

```text
repair_source_anchor
repair_source_owner_ids
```

but the Politics chapter page mounted that enhancer only for Mao.

This meant Xi's immediate R repair-return worked, while a later Return Packet could preserve the failure but lose independent proof of where it should be repaired after `last_location` changed.

This was classified as an **Evidence defect**, not a reason to reopen R or upstream gates.

### Narrow repair

The Current Politics chapter page now mounts the existing generic `PoliticsEvidenceEnhancer` for Xi as well as Mao.

```text
matching Xi Wrong/Uncertain event
→ preserve first-attempt object unchanged
→ attach repair_source_anchor
→ attach Current repair_source_owner_ids
```

No event schema was changed, no evidence logic was duplicated, and Ethics-Law remains untouched pending its own E acceptance.

### Executed browser Evidence proof

Final fresh implementation head: `beed0f112cf19ed73ce159da9a2f75639fe6905b`.

Xi Evidence browser journey: **26/26 checks PASS**.

It proves:

1. clean STABLE creates no Wrong/Uncertain debt and keeps no-mastery semantics explicit;
2. repair evidence retains subject/chapter/unit/question identity;
3. repair provenance includes stable source anchor and source-owner IDs;
4. repair does not rewrite the first-attempt object;
5. after later navigation moves `last_location` to `ch17`, exported `kianos.politics.return_packet.v1` still preserves the original repair event's source provenance;
6. UNCERTAIN remains a distinct evidence state;
7. forced attempt-persistence failure visibly fails closed and cannot manufacture a first attempt or Unit Return.

Final same-head regressions:

- Static Web Politics QA #579 (`34774453094`) → PASS;
- Politics Xi Runtime Journey #6 (`34774453081`) → PASS;
- Politics Xi Evidence Journey #3 (`34774453080`) → PASS;
- Politics Functional First Journey #90 (`34774453110`) → PASS.

### Test correction boundary

The first browser draft attempted to manufacture a wrong answer on a question whose official answer was `ABCD`; no wrong option existed, so the test accidentally produced STABLE. The test was corrected to use a legitimate UNCERTAIN repair event. This was a test-assumption correction, not a product defect and not part of the E repair count.

---

## E boundary / negative space

E PASS does **not** mean:

- STABLE means mastery;
- repair converts historical Wrong/Uncertain evidence into first-attempt success;
- later fresh/holdout transfer has already occurred;
- learner evidence belongs in shared repository truth;
- Kian has studied, transferred or mastered Xi.

Fresh/holdout transfer belongs to later review/mock phases and was not executed as part of this first-round E closure. Future transfer evidence may strengthen or challenge earlier evidence, but must append/reconcile rather than overwrite first-attempt truth.

---

## Stage boundary

```text
S/K/L/P/R/E  CLOSED under Current contracts
U            UNTESTED / learner-only
```

No engineering gate remains active for Xi. The next legitimate action is real learner use. New engineering work requires concrete learner evidence that reopens the earliest responsible gate.

U cannot be accepted from CI, code, browser automation, screenshots, architecture discussion or repository state.

---

## Truth boundaries

### Artifact Truth
- teaching/review assets → `content/politics/learning/xi/`
- Xi Projection adapter → `static-web/src/lib/politicsXiProjection.mjs`
- Unit Return runtime → `static-web/src/lib/politicsUnitReturn.mjs`
- Evidence provenance enhancer → `static-web/src/components/PoliticsEvidenceEnhancer.astro`
- Xi Runtime/Evidence acceptance harnesses → `static-web/scripts/test-politics-xi-runtime.mjs`, `audit-politics-xi-evidence.mjs`, `test-politics-xi-evidence.mjs`
- shared contracts → `content/politics/LEARNING_CONTRACT.md` + `content/politics/INTERACTION_CONTRACT.md`

### Acceptance Truth
This file.

### Learner Truth
Private browser / Return Packet / conversation evidence only.

> **Xi is ready for learner test; it is not yet learner-validated.**
