# Politics Current

Role: Politics lane Work Cursor + subject router  
Parent: root `CURRENT.md`

This file is a compact cursor. It does not own Source Truth, subject Acceptance detail, teaching content, learner progress, or the full UI implementation contract.

---

## Lane Work Cursor

**Learning-engineering scope:** closed through Evidence for all five Politics subjects.  
**Learning blocker:** none.  
**Learning next action:** real Kian use once the current product mapping gate and downstream UI closure are accepted; do not manufacture new semantic work without a concrete defect.

Frozen learner loop:

```text
Orientation / current Natural Unit
→ Chengfeng continuous study on iPad / MarginNote
→ optional close / checkpoint
→ Xiao1000 verification in Astro
→ stable correct → continue cheaply
   OR Wrong / Uncertain → smallest sufficient repair → owning source / Chat → exact return
→ meaningful resume
```

All five subjects remain `S/K/L/P/R/E PASS; U UNTESTED`.

---

## Current Product Blocker

**Current product task:** PR #352 — accept and merge explicit Content → learner-surface mapping.  
**Highest-priority Politics product task:** PR #352 — explicit Content → learner-surface mapping acceptance.

Why it is upstream of Politics UI:

```text
Current knowledge / absorbed Suyi
→ learning_semantics
→ H1–H5 learner attention hierarchy
→ explicit surface mapping
→ Astro faithful rendering only
```

Astro must not infer from generic `Map` / `Chain` / `edges` / Projection field names whether content is parallel, causal, chronological, comparative, hierarchical, or merely structured text.

Current #352 artifact state:

```text
PASS Natural Units              151 total
Explicitly mapped               151 / 151
History                          31 / 31 COMPLETE
Mao                              18 / 18 COMPLETE
Ethics-Law                       21 / 21 COMPLETE
Marxism                          21 / 21 COMPLETE
Xi                               60 / 60 COMPLETE
REFERENCE_ONLY                    9 / 9 remain non-teaching
BLOCKED                           0
```

The mapping artifact is complete. The remaining gate is **acceptance**, not more Xi authoring:

1. current-head automated Politics mapping / browser / functional acceptance must be green;
2. fresh independent relation-fidelity review must confirm the declared primitive/grouping matches Current across all five subjects;
3. only then merge #352 and reopen downstream Politics learner-facing UI work.

Do not reintroduce legacy renderer heuristics or old Golden DOM shapes merely to satisfy stale tests.

### UI / Human Gate boundary

Politics UI must consume the accepted `surface_mapping`; it must not ask Kian to decide content semantics that should already be owned upstream.

A downstream question such as “should this become a flowchart, relation table, hierarchy, timeline, comparison, or something else?” is **not** a UI decision. It means the semantic presentation plan is missing or ambiguous and must return to Content / Projection / Surface Mapping.

Kian's UI review begins only after semantic display ownership is closed. UI may ask about visual/ergonomic implementation that preserves the declared primitive — density, readability, horizontal/vertical composition, spacing, typography, interaction comfort — but not about what the content means or what relation type it carries.

---

## Product / UI Status

Current durable product facts:

- `static-web/POLITICS_PRODUCT_STATUS.md` owns the broader product status and acceptance matrix;
- Xiao1000 Workbench reconciliation is already on `main`; former PR #117 is historical implementation/browser evidence, **not** the active Politics implementation owner;
- Issue #116 still owns broader remaining Politics product-parity closure;
- the 1148 Current-derived Xiao1000 learner explanations remain accepted derived content;
- shared learner shell PR #367 is merged on `main`: global left K rail owns Home / 西综 / 政治 / English, while subject top bars own only subject-internal navigation;
- Politics must **consume** that shared shell after #352 acceptance and must not fork or locally patch shared Shell / global navigation semantics;
- full Mac-wide Politics product acceptance and learner `U` remain open.

After #352 is accepted, resume the downstream chain:

```text
accepted Current / explicit surface mapping
→ consume accepted Shared Shell from main
→ faithful Politics renderer / existing Runtime / Evidence / Repair / Return
→ one clear Politics presentation owner
→ bounded Mac-wide UI optimization
→ real browser / Mac screenshots
→ Kian visual review
→ zero-semantic-diff acceptance
→ real learner use / U
```

Broad Politics UI redesign remains paused until #352 acceptance; architecture inventory / read-only analysis may proceed, but no downstream renderer should become semantic authority before the mapping gate closes.

---

## Productization Read / Handoff Path

For the current mapping acceptance task:

1. PR #352 current exact head;
2. `content/politics/SURFACE_MAPPING_CONTRACT.md` on that candidate;
3. exact Current learning owners referenced by independently sampled Projection units;
4. strict mapping resolver / audit / browser acceptance;
5. independent relation-fidelity result recorded on #352.

For UI work **after #352**:

1. current `main` shared shell implementation / contract;
2. `static-web/POLITICS_PRODUCT_STATUS.md`;
3. `static-web/POLITICS_UI_REVIEW_PROTOCOL.md`;
4. `content/politics/LEARNING_CONTRACT.md` + `INTERACTION_CONTRACT.md`;
5. `static-web/PRESENTATION_CONTRACT.md` + current shared UI/style brief;
6. exact accepted surface-mapping owner / Projection ref;
7. touched Runtime / Evidence / Repair / Return implementation.

Legacy may inform bounded visual/function reference where explicitly authorized, but it is not semantic authority.

---

## Subject Router

| Scope | Work Cursor | Acceptance Truth |
| --- | --- | --- |
| Marxism | `content/politics/learning/marxism/CURRENT.md` | `content/politics/learning/marxism/ACCEPTANCE.md` |
| History | `content/politics/learning/history/CURRENT.md` | `content/politics/learning/history/ACCEPTANCE.md` |
| Mao | `content/politics/learning/mao/CURRENT.md` | `content/politics/learning/mao/ACCEPTANCE.md` |
| Xi | `content/politics/learning/xi/CURRENT.md` | `content/politics/learning/xi/ACCEPTANCE.md` |
| Ethics-Law | `content/politics/learning/ethics-law/CURRENT.md` | `content/politics/learning/ethics-law/ACCEPTANCE.md` |

---

## Hard Boundaries

```text
Suyi       framework / orientation / exactness cross-check input
Chengfeng  continuous first-round mainline on original iPad / MarginNote
Xiao1000   verification / later transfer evidence in Astro
KianOS     orientation / selective Projection / verification / repair companion
Chat       adaptive semantic repair when earned
```

- `Source ownership ≠ Surface ownership`;
- compiled / explicit mapping may select representation but cannot invent knowledge relations;
- `REFERENCE_ONLY` stays non-teaching payload;
- `first_round_exact` stays Current-owned;
- stable correct stays cheap; Wrong/Uncertain opens only the smallest useful repair;
- first-attempt Evidence and exact Return semantics must survive later UI optimization;
- Politics UI modernization must not create a second lecture, second scheduler, or new diagnosis ritual;
- shared Shell / global K rail is platform-owned; Politics owns only its native subject surfaces and internal navigation after the upstream mapping is accepted.

---

## Fresh-Chat Routing

```text
Politics CURRENT
→ learning engineering already closed through E
→ current gate: PR #352 acceptance, not more mapping authoring
→ 151/151 PASS mappings already present; 9/9 REFERENCE_ONLY stay non-teaching
→ current-head automated acceptance + independent relation-fidelity review
→ merge #352
→ consume shared Shell already accepted on main (#367)
→ Politics UI architecture convergence: one renderer path + one Politics presentation owner
→ Mac/browser screenshot acceptance + #116 product closure
→ after accepted rollout: STOP engineering by default → real Kian use / U
```
